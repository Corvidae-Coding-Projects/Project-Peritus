[package]
name = "peritus-web"
description = "Local browser workspace and gateway for the Peritus harness"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"
keywords = ["agents", "web", "workspace"]
categories = ["development-tools"]

[dependencies]
axum = "=0.8.8"
tower = { version = "=0.5.3", features = ["util"] }
tower-http = { version = "=0.6.11", features = ["fs"] }
peritus-app-client = { version = "=0.0.3", path = "../peritus-app-client" }
peritus-app-protocol = { version = "=0.0.3", path = "../peritus-app-protocol" }
peritus-types = { version = "=0.0.3", path = "../../foundation/peritus-types" }
peritus-local-socket = { version = "=0.0.3", path = "../../runtime/peritus-local-socket" }
base64.workspace = true
getrandom.workspace = true
portable-pty.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
tempfile.workspace = true
tokio = { workspace = true, features = ["process"] }
toml.workspace = true

[dev-dependencies]
peritus-codec = { version = "=0.0.3", path = "../../foundation/peritus-codec" }

[package.metadata.peritus]
owner = "G1"
layer = "app"
verification-class = "C"

[lints]
workspace = true
