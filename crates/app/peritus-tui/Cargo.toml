[package]
name = "peritus-tui"
description = "Interactive terminal client for the Peritus daemon"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
keywords = ["agents", "terminal", "tui", "client"]
categories = ["command-line-utilities"]

[dependencies]
base64.workspace = true
crossterm.workspace = true
peritus-app-protocol = { version = "=0.0.1", path = "../peritus-app-protocol" }
peritus-codec = { version = "=0.0.1", path = "../../foundation/peritus-codec" }
peritus-local-socket = { version = "=0.0.1", path = "../../runtime/peritus-local-socket" }
peritus-protocol = { version = "=0.0.1", path = "../../foundation/peritus-protocol" }
peritus-product-runner = { version = "=0.0.1", path = "../peritus-product-runner" }
peritus-run-settlement = { version = "=0.0.1", path = "../../orchestration/peritus-run-settlement" }
peritus-tools-shell = { version = "=0.0.1", path = "../../tools/peritus-tools-shell" }
peritus-types = { version = "=0.0.1", path = "../../foundation/peritus-types" }
ratatui.workspace = true
sha2.workspace = true
thiserror.workspace = true
tokio.workspace = true

[target.'cfg(unix)'.dependencies]
libc.workspace = true

[target.'cfg(windows)'.dependencies]
windows-sys = { workspace = true, features = ["Win32_Storage_FileSystem", "Win32_System_Console"] }

[dev-dependencies]
tempfile.workspace = true

[[bin]]
name = "peritus-tui"
path = "src/main.rs"

[package.metadata.peritus]
owner = "G2"
layer = "app"
verification-class = "C"

[lints]
workspace = true
