//! Boundary and adversarial integration tests for the public conformance API.

mod conformance {
    mod cancellation;
    mod catalog;
    mod harness;
    mod identity;
    mod panic_paths;
    mod runner_paths;
    mod state_catalog;
}
