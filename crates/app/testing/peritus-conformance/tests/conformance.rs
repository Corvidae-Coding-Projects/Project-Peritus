//! Boundary and adversarial integration tests for the public conformance API.

mod conformance {
    mod agent_catalog;
    mod cancellation;
    mod catalog;
    mod gate_catalog;
    mod harness;
    mod identity;
    mod panic_paths;
    mod process_catalog;
    mod provider_catalog;
    mod runner_paths;
    mod sandbox_catalog;
    mod state_catalog;
    mod tool_catalog;
    mod trace_catalog;
}
