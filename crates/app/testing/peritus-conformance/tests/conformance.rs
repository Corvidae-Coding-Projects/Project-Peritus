//! Boundary and adversarial integration tests for the public conformance API.

mod conformance {
    mod cancellation;
    mod catalog;
    mod harness;
    mod identity;
    mod panic_paths;
    mod process_catalog;
    mod runner_paths;
    mod sandbox_catalog;
    mod state_catalog;
}
