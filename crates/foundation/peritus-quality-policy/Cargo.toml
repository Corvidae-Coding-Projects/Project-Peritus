[package]
name = "peritus-quality-policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"
description = "Verified exact-revision acceptance evaluator for Project Peritus"

[dependencies]
peritus-spec = { version = "=0.0.0", path = "../peritus-spec" }
peritus-types = { version = "=0.0.0", path = "../peritus-types" }
vstd.workspace = true

[package.metadata.peritus]
owner = "B2"
layer = "foundation"
verification-class = "V"

[package.metadata.verus]
verify = true

[lints]
workspace = true
