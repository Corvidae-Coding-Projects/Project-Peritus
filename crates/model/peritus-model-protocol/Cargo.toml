[package]
name = "peritus-model-protocol"
description = "Provider-neutral bounded model request and normalized stream protocol for Peritus"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"

[dependencies]
peritus-codec = { version = "=0.0.0", path = "../../foundation/peritus-codec" }
peritus-types = { version = "=0.0.0", path = "../../foundation/peritus-types" }
serde.workspace = true
serde_json.workspace = true
vstd.workspace = true

[package.metadata.peritus]
owner = "C5"
layer = "model"
verification-class = "H"

[package.metadata.verus]
verify = true

[lints]
workspace = true
