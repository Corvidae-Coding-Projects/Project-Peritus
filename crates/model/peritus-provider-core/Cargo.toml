[package]
name = "peritus-provider-core"
description = "Owned HTTP transport, cancellation, retry, and model-provider interfaces for Peritus"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"

[dependencies]
bytes.workspace = true
futures-core.workspace = true
futures-util.workspace = true
peritus-model-protocol = { version = "=0.0.0", path = "../peritus-model-protocol" }
reqwest.workspace = true
tokio = { workspace = true, features = ["io-util", "process"] }
url.workspace = true
zeroize.workspace = true

[dev-dependencies]
peritus-types = { version = "=0.0.0", path = "../../foundation/peritus-types" }
tempfile.workspace = true

[package.metadata.peritus]
owner = "C5"
layer = "model"
verification-class = "H"

[package.metadata.verus]
verify = true

[lints]
workspace = true
