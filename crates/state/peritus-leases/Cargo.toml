[package]
name = "peritus-leases"
description = "Verified generation-bound exclusive mutation leases for Peritus"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"
keywords = ["leases", "formal-verification", "concurrency", "rust"]
categories = ["concurrency", "data-structures"]

[dependencies]
peritus-policy = { version = "=0.0.0", path = "../../foundation/peritus-policy" }
peritus-types = { version = "=0.0.0", path = "../../foundation/peritus-types" }
vstd.workspace = true

[dev-dependencies]
peritus-test-support = { version = "=0.0.0", path = "../../app/testing/peritus-test-support" }

[lints]
workspace = true

[package.metadata.peritus]
owner = "B1"
layer = "state"
verification-class = "H"

[package.metadata.verus]
verify = true
