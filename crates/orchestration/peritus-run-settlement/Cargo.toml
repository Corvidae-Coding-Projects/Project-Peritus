[package]
name = "peritus-run-settlement"
description = "Verified candidate-checkpoint and run-settlement domain for Peritus"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"

[dependencies]
peritus-types = { version = "=0.0.0", path = "../../foundation/peritus-types" }
vstd.workspace = true

[package.metadata.peritus]
owner = "E0"
layer = "orchestration"
verification-class = "V"

[package.metadata.verus]
verify = true

[lints]
workspace = true
