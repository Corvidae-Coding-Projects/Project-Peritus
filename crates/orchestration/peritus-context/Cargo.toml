[package]
name = "peritus-context"
description = "Verified provenance-aware context planning for Peritus"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"

[dependencies]
peritus-codec = { version = "=0.0.0", path = "../../foundation/peritus-codec" }
peritus-policy = { version = "=0.0.0", path = "../../foundation/peritus-policy" }
peritus-role = { version = "=0.0.0", path = "../peritus-role" }
peritus-run-knowledge = { version = "=0.0.0", path = "../peritus-run-knowledge" }
peritus-types = { version = "=0.0.0", path = "../../foundation/peritus-types" }
vstd.workspace = true

[package.metadata.peritus]
owner = "C6"
layer = "orchestration"
verification-class = "H"

[package.metadata.verus]
verify = true

[lints]
workspace = true
