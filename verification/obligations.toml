# Schema contract: verification/README.md#proof-obligation-manifest
# B0, B1, and B2 records are intentionally `in-progress`: the exact symbols and executable proof
# evidence exist, while independent protected-review authorization remains deferred. Nothing in
# this file claims discharge until a registered independent reviewer approves the final identities.
schema = "peritus.verification.obligations"
schema_version = 1
baseline = "A1"

[[entries]]
id = "INV-001"
kind = "invariant"
statement = "Every successful lifecycle reduction is admitted by B0's closed critical-step relation, while every rejection returns the input aggregate unchanged; executable aggregate validation and the command-family matrix cover the complete concrete transition surface."
owning_crate = "peritus-kernel"
source_file = "crates/foundation/peritus-kernel/src/reducer.rs"
symbol = "peritus_kernel::reducer::reducer_result_refines"
status = "in-progress"
dependencies = ["OBL-0114", "OBL-0117"]
live_issue = "#7"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-kernel/src/reducer.rs", symbol = "peritus_kernel::reducer::reducer_result_refines", command = "cargo verus verify --package peritus-kernel --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/foundation/peritus-kernel/tests/reference_model.rs", symbol = "peritus_kernel::tests::reference_model::generated_traces_match_reference_after_each_step_and_first_rejection", command = "cargo test --package peritus-kernel --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-002"
kind = "invariant"
statement = "Genesis starts at event sequence one, and each applied reducer transition advances the exact prior sequence once while binding the emitted event and next aggregate to the prior causal head and exact RevisionTuple."
owning_crate = "peritus-kernel"
source_file = "crates/foundation/peritus-kernel/src/model.rs"
symbol = "peritus_kernel::model::causal_transition_refines"
status = "in-progress"
dependencies = ["OBL-0115"]
live_issue = "#7"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-kernel/src/aggregate.rs", symbol = "peritus_kernel::aggregate::KernelAggregate::genesis_result_refines", command = "cargo verus verify --package peritus-kernel --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-kernel/src/reducer.rs", symbol = "peritus_kernel::reducer::reducer_result_refines", command = "cargo verus verify --package peritus-kernel --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/foundation/peritus-kernel/tests/lifecycle_trace.rs", symbol = "peritus_kernel::tests::lifecycle_trace::complete_writer_action_review_acceptance_trace_is_causal", command = "cargo test --package peritus-kernel --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-005"
kind = "invariant"
statement = "A run can enter Accepted only through the exact AcceptanceAccepted edge from Reviewing and Evaluating; failure, cancellation, exhaustion, rejection, pause, closure, and needs-changes transitions cannot introduce acceptance."
owning_crate = "peritus-kernel"
source_file = "crates/foundation/peritus-kernel/src/model.rs"
symbol = "peritus_kernel::model::no_new_accepted_run"
status = "in-progress"
dependencies = ["INV-003", "INV-004", "OBL-0113", "OBL-0116"]
live_issue = "#7"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-kernel/src/proofs.rs", symbol = "peritus_kernel::proofs::accepted_step_requires_acceptance_event", command = "cargo verus verify --package peritus-kernel --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/foundation/peritus-kernel/tests/transition_matrix.rs", symbol = "peritus_kernel::tests::transition_matrix::run_pause_resume_and_all_non_acceptance_terminal_edges_are_explicit", command = "cargo test --package peritus-kernel --all-targets --all-features --locked" },
  { kind = "property-test", source_file = "crates/foundation/peritus-kernel/tests/adversarial.rs", symbol = "peritus_kernel::tests::adversarial::incomplete_current_evidence_enters_fixer_state_without_implicit_success", command = "cargo test --package peritus-kernel --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-020"
kind = "invariant"
statement = "Every stored attempt, turn, action, review, and waiver resolves to its declared parent chain, and commands with unknown or mismatched parents are rejected without an event."
owning_crate = "peritus-kernel"
source_file = "crates/foundation/peritus-kernel/src/aggregate/validation.rs"
symbol = "peritus_kernel::aggregate::validation::parents_are_valid"
status = "in-progress"
dependencies = ["OBL-0117"]
live_issue = "#7"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/foundation/peritus-kernel/tests/transition_matrix.rs", symbol = "peritus_kernel::tests::transition_matrix::missing_parents_and_illegal_child_edges_fail_without_events", command = "cargo test --package peritus-kernel --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/foundation/peritus-kernel/tests/reference_model.rs", symbol = "peritus_kernel::tests::reference_model::generated_traces_match_reference_after_each_step_and_first_rejection", command = "cargo test --package peritus-kernel --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-003"
kind = "invariant"
statement = "An acceptance decision can be acceptable only when every gate, review, artifact, approval, and waiver observation is bound to the exact requested RevisionTuple."
owning_crate = "peritus-quality-policy"
source_file = "crates/foundation/peritus-quality-policy/src/evidence.rs"
symbol = "peritus_quality_policy::evidence::AcceptanceEvidence::spec_all_current"
status = "in-progress"
dependencies = ["OBL-0111", "OBL-0113"]
live_issue = "#6"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-quality-policy/src/proofs/freshness.rs", symbol = "peritus_quality_policy::proofs::freshness::freshness_implies_exact_revision", command = "cargo verus verify --package peritus-quality-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/foundation/peritus-quality-policy/tests/cases/freshness.rs", symbol = "peritus_quality_policy::tests::cases::freshness::one_field_revision_drift_is_stale_for_every_tuple_dimension", command = "cargo test --package peritus-quality-policy --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-004"
kind = "invariant"
statement = "An acceptable decision implies exact contract binding, fresh observations, complete evaluator phase flags, no typed unmet condition, and passing gate and review observations within the recorded completion limits; adversarial tests refine the remaining contract semantics."
owning_crate = "peritus-quality-policy"
source_file = "crates/foundation/peritus-quality-policy/src/evaluator.rs"
symbol = "peritus_quality_policy::evaluator::accepted_evaluation_contract"
status = "in-progress"
dependencies = ["INV-003", "INV-011", "OBL-0113"]
live_issue = "#6"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-quality-policy/src/evaluator.rs", symbol = "peritus_quality_policy::evaluator::accepted_evaluation_contract", command = "cargo verus verify --package peritus-quality-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/foundation/peritus-quality-policy/tests/cases/happy_path.rs", symbol = "peritus_quality_policy::tests::cases::happy_path::complete_exact_revision_evidence_is_acceptable", command = "cargo test --package peritus-quality-policy --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-011"
kind = "invariant"
statement = "A checked gate graph has unique declared gates, rejects unknown, self, and cyclic dependencies, and exposes a complete deterministic order in which every dependency precedes its dependent gate."
owning_crate = "peritus-spec"
source_file = "crates/foundation/peritus-spec/src/gate.rs"
symbol = "peritus_spec::gate::GateGraph::invariant"
status = "in-progress"
dependencies = ["OBL-0112"]
live_issue = "#6"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-spec/src/gate_model.rs", symbol = "peritus_spec::gate_model::gate_execution_order_is_valid", command = "cargo verus verify --package peritus-spec --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-spec/src/gate_model.rs", symbol = "peritus_spec::gate_model::valid_graph_has_dependency_order_certificate", command = "cargo verus verify --package peritus-spec --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/foundation/peritus-spec/tests/gate_graph.rs", symbol = "peritus_spec::tests::gate_graph::graph_rejects_unknown_dependencies_and_cycles", command = "cargo test --package peritus-spec --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-006"
kind = "invariant"
statement = "For one lease aggregate and generation, every current logical claim identifies the same holder and claim version, and every accepted fence makes every previously current claim non-current."
owning_crate = "peritus-leases"
source_file = "crates/state/peritus-leases/src/model/concrete.rs"
symbol = "peritus_leases::model::concrete::concrete_exclusive"
status = "in-progress"
dependencies = ["OBL-0103", "OBL-0107"]
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-leases/src/proofs/exclusivity.rs", symbol = "peritus_leases::proofs::exclusivity::two_concrete_current_claims_are_identical_in_holder_and_generation", command = "cargo verus verify --package peritus-leases --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/state/peritus-leases/src/proofs/fencing.rs", symbol = "peritus_leases::proofs::fencing::concrete_fence_invalidates_previous_claim", command = "cargo verus verify --package peritus-leases --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "INV-007"
kind = "invariant"
statement = "The immutable role-operation matrix denies mutation to reviewer and evaluator roles, denies acceptance, waiver, amendment, and promotion to writer and fixer roles, and grants no raw-effect authority to the orchestrator role."
owning_crate = "peritus-policy"
source_file = "crates/foundation/peritus-policy/src/model.rs"
symbol = "peritus_policy::model::role_permits"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/role_separation.rs", symbol = "peritus_policy::proofs::role_separation::reviewer_cannot_mutate", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/role_separation.rs", symbol = "peritus_policy::proofs::role_separation::evaluator_cannot_mutate", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/role_separation.rs", symbol = "peritus_policy::proofs::role_separation::writer_cannot_accept_or_amend", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/role_separation.rs", symbol = "peritus_policy::proofs::role_separation::fixer_cannot_accept_or_amend", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/role_separation.rs", symbol = "peritus_policy::proofs::role_separation::orchestrator_has_no_raw_effect_authority", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "INV-008"
kind = "invariant"
statement = "Every successful capability use preserves the exact actor, role, environment, revision, and permission pair from its issued scope and decrements a finite positive use count exactly once; every rejected use preserves the prior capability."
owning_crate = "peritus-policy"
source_file = "crates/foundation/peritus-policy/src/model.rs"
symbol = "peritus_policy::model::capability_use_success"
status = "in-progress"
dependencies = ["INV-007", "OBL-0101", "OBL-0103"]
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/scope.rs", symbol = "peritus_policy::proofs::scope::identical_scope_dimensions_are_preserved", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/refinement.rs", symbol = "peritus_policy::proofs::refinement::one_success_decrements_once", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/refinement.rs", symbol = "peritus_policy::proofs::refinement::rejected_use_preserves_remaining", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "INV-009"
kind = "invariant"
statement = "Every reachable approval model state has at most one semantic resolution and at most one approve-once consumption, with exact replay preserving the state and terminal phases rejecting a second semantic result."
owning_crate = "peritus-approval"
source_file = "crates/state/peritus-approval/src/model.rs"
symbol = "peritus_approval::model::inv_009"
status = "in-progress"
dependencies = ["INV-008", "OBL-0103", "OBL-0109", "OBL-0110"]
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-approval/src/proofs/refinement.rs", symbol = "peritus_approval::proofs::refinement::reachable_preserves_inv_009", command = "cargo verus verify --package peritus-approval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/state/peritus-approval/src/proofs/binding.rs", symbol = "peritus_approval::proofs::binding::consume_once_preserves_digest", command = "cargo verus verify --package peritus-approval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "INV-012"
kind = "invariant"
statement = "Every accepted budget transition preserves ledger well-formedness, monotonic consumption and observation high-water marks, stable identities, ancestor charge propagation, and conservative reservation and delegation accounting; every rejected transition preserves the input ledger."
owning_crate = "peritus-budget"
source_file = "crates/foundation/peritus-budget/src/reachability.rs"
symbol = "peritus_budget::reachability::complete_refinement"
status = "in-progress"
dependencies = ["OBL-0104", "OBL-0105", "OBL-0106"]
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-budget/src/reachability.rs", symbol = "peritus_budget::reachability::accepted_step_is_valid_and_complete", command = "cargo verus verify --package peritus-budget --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-budget/src/reachability/total.rs", symbol = "peritus_budget::reachability::total::rejected_is_budget_step", command = "cargo verus verify --package peritus-budget --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "INV-022"
kind = "invariant"
statement = "Ordinary policy-layer composition is pointwise no broader than its protected ceiling and prior layers, while every accepted amendment is an exact one-tier successor bounded by the rebound higher-tier ceiling and immutable denial set."
owning_crate = "peritus-policy"
source_file = "crates/foundation/peritus-policy/src/monotonicity_model.rs"
symbol = "peritus_policy::monotonicity_model::decision_respects_allowed_query_set"
status = "in-progress"
dependencies = ["OBL-0100", "OBL-0102"]
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/monotonicity.rs", symbol = "peritus_policy::proofs::monotonicity::evaluator_cannot_broaden_allowed_queries", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/amendment/preview.rs", symbol = "peritus_policy::amendment::preview::preview_amendment_result_is_exact", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0100"
kind = "contract"
statement = "An applicable deny has the least decision rank, dominates every other policy decision under composition, and prevents whole-request capability issuance."
owning_crate = "peritus-policy"
source_file = "crates/foundation/peritus-policy/src/proofs/monotonicity.rs"
symbol = "peritus_policy::proofs::monotonicity::deny_wins"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/monotonicity.rs", symbol = "peritus_policy::proofs::monotonicity::deny_wins", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0101"
kind = "contract"
statement = "Authorization of independent resource and capability atoms never authorizes an unlisted Cartesian resource-capability pair."
owning_crate = "peritus-policy"
source_file = "crates/foundation/peritus-policy/src/proofs/scope.rs"
symbol = "peritus_policy::proofs::scope::independent_atoms_do_not_imply_cartesian_authority"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/scope.rs", symbol = "peritus_policy::proofs::scope::independent_atoms_do_not_imply_cartesian_authority", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0102"
kind = "contract"
statement = "A successful policy amendment has an exact successor identity, replaces exactly its declared tier, rebinds all retained components to that successor revision, and remains within the protected ceiling and immutable denial set."
owning_crate = "peritus-policy"
source_file = "crates/foundation/peritus-policy/src/amendment/preview.rs"
symbol = "peritus_policy::amendment::preview::preview_amendment_result_is_exact"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/amendment/preview.rs", symbol = "peritus_policy::amendment::preview::preview_amendment_result_is_exact", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0103"
kind = "contract"
statement = "An accepted authority-time observation has the same epoch and a tick no lower than the prior floor; an epoch discontinuity or regressing tick returns an unchanged linear floor and no accepted authority."
owning_crate = "peritus-policy"
source_file = "crates/foundation/peritus-policy/src/time.rs"
symbol = "peritus_policy::time::AuthorityTimeState::spec_accepts"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/proofs/refinement.rs", symbol = "peritus_policy::proofs::refinement::accepted_time_never_regresses", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-policy/src/time.rs", symbol = "peritus_policy::time::AuthorityTimeState::spec_accepts", command = "cargo verus verify --package peritus-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0104"
kind = "invariant"
statement = "Every well-formed budget ledger conserves each immutable account limit across consumed, reserved, delegated, and available quantities."
owning_crate = "peritus-budget"
source_file = "crates/foundation/peritus-budget/src/model.rs"
symbol = "peritus_budget::model::ledger_well_formed"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-budget/src/reachability.rs", symbol = "peritus_budget::reachability::accepted_step_is_valid_and_complete", command = "cargo verus verify --package peritus-budget --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0105"
kind = "invariant"
statement = "For every accepted usage observation, each reservation cumulative observation is nondecreasing and only its positive delta is charged; exact replay charges nothing twice."
owning_crate = "peritus-budget"
source_file = "crates/foundation/peritus-budget/src/model.rs"
symbol = "peritus_budget::model::ledger_high_water_monotonic"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-budget/src/reachability/reducer_proofs/observation.rs", symbol = "peritus_budget::reachability::reducer_proofs::observation::observation_refines", command = "cargo verus verify --package peritus-budget --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0106"
kind = "invariant"
statement = "Child allocation cannot exceed parent availability, and every accepted descendant consumption delta is propagated exactly through each ancestor in its lineage."
owning_crate = "peritus-budget"
source_file = "crates/foundation/peritus-budget/src/refinement_model.rs"
symbol = "peritus_budget::refinement_model::ancestor_consumption_propagates"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-budget/src/reachability.rs", symbol = "peritus_budget::reachability::accepted_step_is_valid_and_complete", command = "cargo verus verify --package peritus-budget --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-budget/src/reachability/guards.rs", symbol = "peritus_budget::reachability::guards::request_capacity_from_available", command = "cargo verus verify --package peritus-budget --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0107"
kind = "invariant"
statement = "Release, expiry, holder loss, clock discontinuity, revocation, and boundary retirement advance or retire the lease generation before availability, invalidating every prior current claim."
owning_crate = "peritus-leases"
source_file = "crates/state/peritus-leases/src/proofs/fencing.rs"
symbol = "peritus_leases::proofs::fencing::concrete_fence_invalidates_previous_claim"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-leases/src/proofs/fencing.rs", symbol = "peritus_leases::proofs::fencing::concrete_fence_invalidates_previous_claim", command = "cargo verus verify --package peritus-leases --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/state/peritus-leases/src/proofs/fencing.rs", symbol = "peritus_leases::proofs::fencing::accepted_boundary_fence_is_terminal", command = "cargo verus verify --package peritus-leases --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0108"
kind = "contract"
statement = "Every accepted logical lease-use transition is bounded by the exact current lease claim and by the exact actor, role, environment, revision, permission, action, and time authority in the consumed capability use."
owning_crate = "peritus-leases"
source_file = "crates/state/peritus-leases/src/model.rs"
symbol = "peritus_leases::model::exact_authority_intersection"
status = "in-progress"
dependencies = ["INV-007", "INV-008"]
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-leases/src/proofs/authority.rs", symbol = "peritus_leases::proofs::authority::exact_intersection_is_no_broader_than_both_inputs", command = "cargo verus verify --package peritus-leases --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/state/peritus-leases/src/proofs/authority.rs", symbol = "peritus_leases::proofs::authority::executable_logical_use_remains_bound_to_current_claim", command = "cargo verus verify --package peritus-leases --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0109"
kind = "contract"
statement = "Every accepted approval decision and approve-once use preserves the exact request, action, policy revision, decision, registry revision, and amendment identity digests selected by authenticated validation."
owning_crate = "peritus-approval"
source_file = "crates/state/peritus-approval/src/state/use_specification.rs"
symbol = "peritus_approval::state::use_specification::consume_once_result_relation"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-approval/src/proofs/binding.rs", symbol = "peritus_approval::proofs::binding::consume_once_preserves_digest", command = "cargo verus verify --package peritus-approval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/state/peritus-approval/src/state/specification.rs", symbol = "peritus_approval::state::specification::consume_once_refines", command = "cargo verus verify --package peritus-approval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0110"
kind = "invariant"
statement = "An exact repeated approval response is idempotent, and no terminal approval phase can transition to a different semantic resolution or create a second consumption."
owning_crate = "peritus-approval"
source_file = "crates/state/peritus-approval/src/proofs/replay.rs"
symbol = "peritus_approval::proofs::replay::terminal_state_cannot_change"
status = "in-progress"
dependencies = []
live_issue = "#5"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-approval/src/proofs/replay.rs", symbol = "peritus_approval::proofs::replay::exact_replay_is_idempotent", command = "cargo verus verify --package peritus-approval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/state/peritus-approval/src/proofs/replay.rs", symbol = "peritus_approval::proofs::replay::terminal_state_cannot_change", command = "cargo verus verify --package peritus-approval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0111"
kind = "contract"
statement = "A successful contract binding preserves the immutable contract identity and digest, stores the exact supplied RevisionTuple, and exists only when the tuple names that acceptance specification."
owning_crate = "peritus-spec"
source_file = "crates/foundation/peritus-spec/src/contract.rs"
symbol = "peritus_spec::contract::ContractBinding::invariant"
status = "in-progress"
dependencies = []
live_issue = "#6"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-spec/src/contract.rs", symbol = "peritus_spec::contract::ContractBinding::invariant", command = "cargo verus verify --package peritus-spec --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/foundation/peritus-spec/tests/binding.rs", symbol = "peritus_spec::tests::binding::binding_requires_the_tuple_to_name_the_exact_contract", command = "cargo test --package peritus-spec --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0112"
kind = "contract"
statement = "Every successfully constructed GateGraph satisfies the executable-refined validity predicate: declared and scheduled gate identities are unique and complete, every dependency precedes its gate, and the stored Kahn order is deterministic."
owning_crate = "peritus-spec"
source_file = "crates/foundation/peritus-spec/src/gate.rs"
symbol = "peritus_spec::gate::GateGraph::new"
status = "in-progress"
dependencies = []
live_issue = "#6"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-spec/src/gate.rs", symbol = "peritus_spec::gate::GateGraph::invariant", command = "cargo verus verify --package peritus-spec --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-spec/src/gate_model.rs", symbol = "peritus_spec::gate_model::valid_graph_has_dependency_order_certificate", command = "cargo verus verify --package peritus-spec --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0113"
kind = "contract"
statement = "The executable acceptance evaluator can report acceptable only with exact contract binding and freshness, complete phase flags, concrete gate-attempt and review-cycle predicates at the recorded limits, and an empty typed unmet-condition sequence."
owning_crate = "peritus-quality-policy"
source_file = "crates/foundation/peritus-quality-policy/src/evaluator.rs"
symbol = "peritus_quality_policy::evaluator::evaluate_acceptance"
status = "in-progress"
dependencies = ["OBL-0111"]
live_issue = "#6"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-quality-policy/src/evaluator.rs", symbol = "peritus_quality_policy::evaluator::accepted_evaluation_contract", command = "cargo verus verify --package peritus-quality-policy --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/foundation/peritus-quality-policy/tests/cases/happy_path.rs", symbol = "peritus_quality_policy::tests::cases::happy_path::complete_exact_revision_evidence_is_acceptable", command = "cargo test --package peritus-quality-policy --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0114"
kind = "contract"
statement = "The executable B0 reducer directly refines the closed concrete session and acceptance transition relation, and every rejected outcome contains the unchanged input aggregate."
owning_crate = "peritus-kernel"
source_file = "crates/foundation/peritus-kernel/src/reducer.rs"
symbol = "peritus_kernel::reducer::reducer_result_refines"
status = "in-progress"
dependencies = []
live_issue = "#7"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-kernel/src/reducer.rs", symbol = "peritus_kernel::reducer::reducer_result_refines", command = "cargo verus verify --package peritus-kernel --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0115"
kind = "contract"
statement = "Successful genesis and reducer results preserve exact revision freshness, bind every event to the expected predecessor, and advance sequence one time."
owning_crate = "peritus-kernel"
source_file = "crates/foundation/peritus-kernel/src/aggregate.rs"
symbol = "peritus_kernel::aggregate::KernelAggregate::genesis_result_refines"
status = "in-progress"
dependencies = []
live_issue = "#7"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-kernel/src/aggregate.rs", symbol = "peritus_kernel::aggregate::KernelAggregate::genesis_result_refines", command = "cargo verus verify --package peritus-kernel --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-kernel/src/model.rs", symbol = "peritus_kernel::model::causal_transition_refines", command = "cargo verus verify --package peritus-kernel --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0116"
kind = "invariant"
statement = "Every accepted run is created by AcceptanceAccepted from current Evaluating evidence, while every other applied event proves that it introduced no accepted run."
owning_crate = "peritus-kernel"
source_file = "crates/foundation/peritus-kernel/src/proofs.rs"
symbol = "peritus_kernel::proofs::accepted_step_requires_acceptance_event"
status = "in-progress"
dependencies = ["OBL-0113"]
live_issue = "#7"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/foundation/peritus-kernel/src/proofs.rs", symbol = "peritus_kernel::proofs::accepted_step_requires_acceptance_event", command = "cargo verus verify --package peritus-kernel --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/foundation/peritus-kernel/src/reducer.rs", symbol = "peritus_kernel::reducer::reducer_result_refines", command = "cargo verus verify --package peritus-kernel --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
]

[[entries]]
id = "OBL-0117"
kind = "contract"
statement = "The executable aggregate validator rejects duplicate identities, more than one live run, accepted-state mismatches, terminal-state mismatches, and missing or mismatched parent chains."
owning_crate = "peritus-kernel"
source_file = "crates/foundation/peritus-kernel/src/aggregate/validation.rs"
symbol = "peritus_kernel::aggregate::validation::is_valid"
status = "in-progress"
dependencies = []
live_issue = "#7"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/foundation/peritus-kernel/tests/transition_matrix.rs", symbol = "peritus_kernel::tests::transition_matrix::missing_parents_and_illegal_child_edges_fail_without_events", command = "cargo test --package peritus-kernel --all-targets --all-features --locked" },
  { kind = "property-test", source_file = "crates/foundation/peritus-kernel/tests/adversarial.rs", symbol = "peritus_kernel::tests::adversarial::revision_forks_and_replayed_identities_preserve_the_aggregate", command = "cargo test --package peritus-kernel --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0118"
kind = "refinement"
statement = "C0 atomically commits the exact B0 event and exact CAS successor state once, exposes the successor only through an opaque post-commit receipt, and reconstructs the same aggregate by checked replay after restart."
owning_crate = "peritus-journal"
source_file = "crates/state/peritus-journal/src/verified.rs"
symbol = "peritus_journal::verified::committed_state_successor"
status = "in-progress"
dependencies = ["OBL-0114", "OBL-0115", "OBL-0117"]
live_issue = "#9"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-journal/src/verified.rs", symbol = "peritus_journal::verified::committed_state_successor", command = "cargo verus verify --package peritus-journal --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/state/peritus-journal/tests/kernel_replay.rs", symbol = "peritus_journal::tests::kernel_replay::genesis_and_transition_recover_exactly_after_journal_restart", command = "cargo test --package peritus-journal --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0119"
kind = "refinement"
statement = "C0 commits capability, budget, reservation, lease, and approval successors exactly once under durable CAS, and CancelHeld additionally requires the matching committed begin lineage plus a current non-activation observation."
owning_crate = "peritus-journal"
source_file = "crates/state/peritus-journal/src/verified.rs"
symbol = "peritus_journal::verified::committed_state_successor"
status = "in-progress"
dependencies = ["OBL-0103", "OBL-0105", "OBL-0106", "OBL-0107", "OBL-0110"]
live_issue = "#9"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-journal/src/verified.rs", symbol = "peritus_journal::verified::committed_state_successor", command = "cargo verus verify --package peritus-journal --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/state/peritus-journal/tests/b1_adapters.rs", symbol = "peritus_journal::tests::b1_adapters::capability_use_is_exposed_only_with_its_exact_durable_successor", command = "cargo test --package peritus-journal --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/state/peritus-journal/tests/b1_adapters.rs", symbol = "peritus_journal::tests::b1_adapters::held_budget_cancellation_requires_and_consumes_a_current_durable_non_activation_observation", command = "cargo test --package peritus-journal --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/state/peritus-journal/tests/b1_adapters.rs", symbol = "peritus_journal::tests::b1_adapters::lease_mint_and_acquisition_follow_the_verified_version_cas", command = "cargo test --package peritus-journal --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/state/peritus-journal/tests/approval_registry.rs", symbol = "peritus_journal::tests::approval_registry::signed_approval_rejects_same_revision_wrong_digest_and_commits_exact_snapshot", command = "cargo test --package peritus-journal --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0120"
kind = "refinement"
statement = "The durable credential registry advances an exact successor revision and a strictly increasing global generation, binds the canonical bytes of the checked B1 snapshot, and rejects approval outcomes authenticated against any other snapshot digest."
owning_crate = "peritus-journal"
source_file = "crates/state/peritus-journal/src/verified.rs"
symbol = "peritus_journal::verified::registry_advance"
status = "in-progress"
dependencies = ["OBL-0107"]
live_issue = "#9"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-journal/src/verified.rs", symbol = "peritus_journal::verified::registry_advance", command = "cargo verus verify --package peritus-journal --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/state/peritus-journal/tests/authority_adapters.rs", symbol = "peritus_journal::tests::authority_adapters::registry_rejects_non_increasing_currentness", command = "cargo test --package peritus-journal --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/state/peritus-journal/tests/approval_registry.rs", symbol = "peritus_journal::tests::approval_registry::signed_approval_rejects_same_revision_wrong_digest_and_commits_exact_snapshot", command = "cargo test --package peritus-journal --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0121"
kind = "refinement"
statement = "Authority-clock allocation is one durable compare-and-swap whose successful observation is the exact positive successor epoch; stale expectations and overflow cannot allocate or reuse an epoch."
owning_crate = "peritus-journal"
source_file = "crates/state/peritus-journal/src/verified.rs"
symbol = "peritus_journal::verified::authority_epoch_successor"
status = "in-progress"
dependencies = ["OBL-0103"]
live_issue = "#9"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-journal/src/verified.rs", symbol = "peritus_journal::verified::authority_epoch_successor", command = "cargo verus verify --package peritus-journal --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/state/peritus-journal/tests/authority_adapters.rs", symbol = "peritus_journal::tests::authority_adapters::authority_clock_rejects_stale_allocation_and_survives_restart", command = "cargo test --package peritus-journal --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0122"
kind = "refinement"
statement = "C0 admits evidence only from a matching digest-verified committed journal record and finalized artifacts, preserves canonical causal ancestry, and marks evidence stale when any exact RevisionTuple component drifts or a later invalidation is recorded."
owning_crate = "peritus-evidence"
source_file = "crates/state/peritus-evidence/src/verified.rs"
symbol = "peritus_evidence::verified::revisions_equal"
status = "in-progress"
dependencies = ["OBL-0111", "OBL-0113"]
live_issue = "#9"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/state/peritus-evidence/src/verified.rs", symbol = "peritus_evidence::verified::revision_fields_match", command = "cargo verus verify --package peritus-evidence --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/state/peritus-evidence/tests/admission.rs", symbol = "peritus_evidence::tests::admission::valid_admission_is_atomic_idempotent_and_survives_restart", command = "cargo test --package peritus-evidence --all-targets --all-features --locked" },
  { kind = "property-test", source_file = "crates/state/peritus-evidence/tests/freshness.rs", symbol = "peritus_evidence::tests::freshness::every_revision_tuple_component_drifts_independently", command = "cargo test --package peritus-evidence --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/state/peritus-evidence/tests/freshness.rs", symbol = "peritus_evidence::tests::freshness::explicit_later_invalidation_is_durable_and_dominates_revision_freshness", command = "cargo test --package peritus-evidence --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0123"
kind = "refinement"
statement = "Every C1 workspace effect target exactly matches the workspace binding, B0 authorization witness, action intent, B1 capability permission, and generation-fenced lease scope by nominal WorkspaceId, ResourceId, and EnvironmentId before any filesystem effect."
owning_crate = "peritus-workspace"
source_file = "crates/runtime/peritus-workspace/src/refinement.rs"
symbol = "peritus_workspace::refinement::accepted_target_is_exact_authorized_resource"
status = "in-progress"
dependencies = ["OBL-0101", "OBL-0108", "OBL-0114", "OBL-0119"]
live_issue = "#10"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-workspace/src/refinement.rs", symbol = "peritus_workspace::refinement::accepted_target_is_exact_authorized_resource", command = "cargo verus verify --package peritus-workspace --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/runtime/peritus-workspace/src/verified.rs", symbol = "peritus_workspace::verified::tests::resource_identity_rejects_each_independent_mismatch", command = "cargo test --package peritus-workspace --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-workspace/tests/authorized_gateway.rs", symbol = "peritus_workspace::tests::authorized_gateway::exact_committed_receipts_are_required_before_real_patch_effect", command = "cargo test --package peritus-workspace --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0124"
kind = "refinement"
statement = "The target-owned C1 gateway alone constructs its private one-use mutation permit after exact current committed B0 dispatch, B1 capability and lease-use, C0 receipt, resource, actor, session, revision, generation, and authority-time facts all match; every rejected request has no target effect."
owning_crate = "peritus-workspace"
source_file = "crates/runtime/peritus-workspace/src/refinement.rs"
symbol = "peritus_workspace::refinement::permit_implies_complete_committed_authority"
status = "in-progress"
dependencies = ["OBL-0103", "OBL-0108", "OBL-0114", "OBL-0118", "OBL-0119"]
live_issue = "#10"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-workspace/src/refinement.rs", symbol = "peritus_workspace::refinement::permit_implies_complete_committed_authority", command = "cargo verus verify --package peritus-workspace --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/runtime/peritus-workspace/src/verified.rs", symbol = "peritus_workspace::verified::tests::authority_gate_rejects_every_missing_fact", command = "cargo test --package peritus-workspace --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-workspace/tests/authorized_gateway.rs", symbol = "peritus_workspace::tests::authorized_gateway::exact_committed_receipts_are_required_before_real_patch_effect", command = "cargo test --package peritus-workspace --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0125"
kind = "refinement"
statement = "C1 classifies a workspace clean only from a complete exact post-fence correlation to its observed scope, generation, and prior holder plus clean transaction and Git state; mismatches and incomplete observations remain typed fenced, dirty, or indeterminate and cannot independently claim SafeToAcquire."
owning_crate = "peritus-workspace"
source_file = "crates/runtime/peritus-workspace/src/refinement.rs"
symbol = "peritus_workspace::refinement::safe_assessment_requires_exact_complete_post_fence_observation"
status = "in-progress"
dependencies = ["OBL-0107", "OBL-0119"]
live_issue = "#10"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-workspace/src/refinement.rs", symbol = "peritus_workspace::refinement::safe_assessment_requires_exact_complete_post_fence_observation", command = "cargo verus verify --package peritus-workspace --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-workspace/src/reconcile_tests.rs", symbol = "peritus_workspace::reconcile_tests::post_fence_classification_never_guesses_safety", command = "cargo test --package peritus-workspace --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-workspace/tests/production_conformance.rs", symbol = "peritus_workspace::tests::production_conformance::production_workspace_passes_a2_git_patch_snapshot_and_recovery_suite", command = "cargo test --package peritus-workspace --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-013"
kind = "invariant"
statement = "Every C2 execution is durably owned by one exact process identity and accepts at most one terminal result only after the owned process tree is quiescent and all support tasks are joined."
owning_crate = "peritus-process"
source_file = "crates/runtime/peritus-process/src/refinement.rs"
symbol = "peritus_process::refinement::terminal_result_implies_unique_owned_completion"
status = "in-progress"
dependencies = ["OBL-0129"]
live_issue = "#11"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-process/src/refinement.rs", symbol = "peritus_process::refinement::terminal_result_implies_unique_owned_completion", command = "cargo verus verify --package peritus-process --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-process/tests/process_integration/control_lifecycle.rs", symbol = "peritus_process::tests::process_integration::control_lifecycle::cancellation_publishes_one_terminal_owned_tree_result", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-process/tests/process_conformance.rs", symbol = "peritus_process::tests::process_conformance::production_process_passes_complete_a2_suite_with_fresh_case_subjects", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0126"
kind = "refinement"
statement = "The C2 target gateway alone constructs its private one-use execution permit after exact current committed B0 dispatch, B1 capability use, active-effect budget begin, conditional lease use, C0 receipts, authority time, revision, owner lineage, sandbox, backend, preparation, and execution-plan facts all match; every rejection has no target effect."
owning_crate = "peritus-process"
source_file = "crates/runtime/peritus-process/src/refinement.rs"
symbol = "peritus_process::refinement::permit_implies_complete_execution_authority"
status = "in-progress"
dependencies = ["OBL-0103", "OBL-0108", "OBL-0114", "OBL-0118", "OBL-0119"]
live_issue = "#11"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-process/src/refinement.rs", symbol = "peritus_process::refinement::permit_implies_complete_execution_authority", command = "cargo verus verify --package peritus-process --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-process/tests/process_integration.rs", symbol = "peritus_process::tests::process_integration::exact_committed_authority_launches_separate_bounded_pipe_streams", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
  { kind = "property-test", source_file = "crates/runtime/peritus-process/tests/process_conformance.rs", symbol = "peritus_process::tests::process_conformance::production_process_passes_complete_a2_suite_with_fresh_case_subjects", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0127"
kind = "refinement"
statement = "C2 holder-quiescence evidence implies exact fenced-claim and prior-holder correlation, a complete durable-registry inspection, zero matching live or unresolved processes, complete owned-tree cleanup, and joined support tasks."
owning_crate = "peritus-process"
source_file = "crates/runtime/peritus-process/src/refinement.rs"
symbol = "peritus_process::refinement::holder_evidence_implies_exact_complete_quiescence"
status = "in-progress"
dependencies = ["OBL-0107", "OBL-0119"]
live_issue = "#11"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-process/src/refinement.rs", symbol = "peritus_process::refinement::holder_evidence_implies_exact_complete_quiescence", command = "cargo verus verify --package peritus-process --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-process/tests/process_integration.rs", symbol = "peritus_process::tests::process_integration::holder_quiescence_requires_exact_terminal_correlated_manifests", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0128"
kind = "refinement"
statement = "Every checked C2 sandbox plan admits every requested capability domain within its declared contract without broadening, and backend admission succeeds only when the selected profile and descriptor cover every enforcement feature required by that complete plan."
owning_crate = "peritus-sandbox"
source_file = "crates/runtime/peritus-sandbox/src/refinement.rs"
symbol = "peritus_sandbox::refinement::checked_plan_is_no_broader_than_contract"
status = "in-progress"
dependencies = []
live_issue = "#11"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-sandbox/src/refinement.rs", symbol = "peritus_sandbox::refinement::checked_plan_is_no_broader_than_contract", command = "cargo verus verify --package peritus-sandbox --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/runtime/peritus-sandbox/src/refinement.rs", symbol = "peritus_sandbox::refinement::admitted_backend_covers_required_features", command = "cargo verus verify --package peritus-sandbox --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-sandbox/tests/backend_reference.rs", symbol = "peritus_sandbox::tests::backend_reference::admission_requires_default_filesystem_and_descendant_denial_controls", command = "cargo test --package peritus-sandbox --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-sandbox/tests/contracts.rs", symbol = "peritus_sandbox::tests::contracts::terminal_bounds_validate_and_reject_each_one_over_requirement", command = "cargo test --package peritus-sandbox --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-sandbox/tests/production_conformance.rs", symbol = "peritus_sandbox::tests::production_conformance::production_reference_adapter_passes_the_complete_a2_sandbox_suite", command = "cargo test --package peritus-sandbox --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0129"
kind = "refinement"
statement = "C2 lifecycle and output accounting accepts one first-trigger terminal classification, one terminal result, monotonic bounded observations, exact observed-retained-dropped byte conservation, complete owned-tree cleanup, and joined support tasks."
owning_crate = "peritus-process"
source_file = "crates/runtime/peritus-process/src/refinement.rs"
symbol = "peritus_process::refinement::terminal_result_implies_unique_owned_completion"
status = "in-progress"
dependencies = []
live_issue = "#11"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-process/src/refinement.rs", symbol = "peritus_process::refinement::terminal_result_implies_unique_owned_completion", command = "cargo verus verify --package peritus-process --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-process/tests/process_integration/control_lifecycle.rs", symbol = "peritus_process::tests::process_integration::control_lifecycle::cancellation_publishes_one_terminal_owned_tree_result", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
  { kind = "property-test", source_file = "crates/runtime/peritus-process/tests/process_conformance.rs", symbol = "peritus_process::tests::process_conformance::production_process_passes_complete_a2_suite_with_fresh_case_subjects", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0130"
kind = "refinement"
statement = "Every C3 native preparation returned to C2 exists only after durable execution-authority consumption and exactly binds the checked sandbox plan, admitted descriptor and support set, preparation digest, current platform, and checksummed helper manifest."
owning_crate = "peritus-process"
source_file = "crates/runtime/peritus-process/src/refinement.rs"
symbol = "peritus_process::refinement::native_preparation_implies_complete_binding"
status = "in-progress"
dependencies = ["OBL-0126", "OBL-0128"]
live_issue = "#12"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-process/src/refinement.rs", symbol = "peritus_process::refinement::native_preparation_implies_complete_binding", command = "cargo verus verify --package peritus-process --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-process/tests/process_integration/native_backend/lifecycle.rs", symbol = "peritus_process::tests::process_integration::native_backend::lifecycle::exact_restricted_authority_runs_through_the_helper_and_releases_the_session", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0131"
kind = "refinement"
statement = "Every C3 managed-network allow, DNS answer, redirect successor, and credential injection is equal to or narrower than the exact checked C2 network and secret authority; deny precedence and explicit special-address authority remain dominant."
owning_crate = "peritus-network"
source_file = "crates/runtime/peritus-network/src/refinement.rs"
symbol = "peritus_network::refinement::managed_network_allow_implies_checked_allow"
status = "in-progress"
dependencies = ["OBL-0128"]
live_issue = "#12"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-network/src/refinement.rs", symbol = "peritus_network::refinement::managed_network_allow_implies_checked_allow", command = "cargo verus verify --package peritus-network --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/runtime/peritus-network/tests/network/policy.rs", symbol = "peritus_network::tests::network::policy::canonical_matching_denies_and_special_addresses_are_narrowed", command = "cargo test --package peritus-network --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-network/tests/network.rs", symbol = "peritus_network::tests::network::managed_proxy_denies_unlisted_destination_before_resolution_or_connect", command = "cargo test --package peritus-network --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0132"
kind = "refinement"
statement = "Every C3 secret delivery uses the exact authorized reference, owner and destination scope under one live bounded lease, and revocation, expiry, use exhaustion, or binding drift prevents material exposure."
owning_crate = "peritus-secrets"
source_file = "crates/runtime/peritus-secrets/src/refinement.rs"
symbol = "peritus_secrets::refinement::delivered_secret_implies_exact_live_lease"
status = "in-progress"
dependencies = ["OBL-0128"]
live_issue = "#12"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-secrets/src/refinement.rs", symbol = "peritus_secrets::refinement::delivered_secret_implies_exact_live_lease", command = "cargo verus verify --package peritus-secrets --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-secrets/tests/secrets.rs", symbol = "peritus_secrets::tests::secrets::leases_reject_drift_expiry_reuse_and_revocation", command = "cargo test --package peritus-secrets --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-secrets/tests/secrets.rs", symbol = "peritus_secrets::tests::secrets::every_delivery_mode_is_owned_redacted_and_idempotently_cleaned", command = "cargo test --package peritus-secrets --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0133"
kind = "refinement"
statement = "Complete C3 native teardown implies the complete owned process tree is quiescent, termination and release are ordered, every backend, proxy, and secret resource is released, and every support task is joined before terminal success."
owning_crate = "peritus-process"
source_file = "crates/runtime/peritus-process/src/refinement.rs"
symbol = "peritus_process::refinement::complete_native_teardown_releases_every_resource"
status = "in-progress"
dependencies = ["INV-013", "OBL-0129"]
live_issue = "#12"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-process/src/refinement.rs", symbol = "peritus_process::refinement::complete_native_teardown_releases_every_resource", command = "cargo verus verify --package peritus-process --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-process/tests/process_integration/native_backend/lifecycle.rs", symbol = "peritus_process::tests::process_integration::native_backend::lifecycle::exact_restricted_authority_runs_through_the_helper_and_releases_the_session", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0134"
kind = "refinement"
statement = "An unsupported, mismatched, unadmitted, wrong-platform, or not-yet-consumed native preparation has zero process, network, secret, or activation effects; a post-consumption preparation failure is durably terminal and cannot replay."
owning_crate = "peritus-process"
source_file = "crates/runtime/peritus-process/src/refinement.rs"
symbol = "peritus_process::refinement::invalid_native_preparation_has_no_effect"
status = "in-progress"
dependencies = ["OBL-0126", "OBL-0128"]
live_issue = "#12"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/runtime/peritus-process/src/refinement.rs", symbol = "peritus_process::refinement::invalid_native_preparation_has_no_effect", command = "cargo verus verify --package peritus-process --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/runtime/peritus-process/tests/process_integration/native_backend/rejection.rs", symbol = "peritus_process::tests::process_integration::native_backend::rejection::descriptor_drift_never_calls_prepare_or_consumes_authority", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/runtime/peritus-process/tests/process_integration/native_backend/rejection.rs", symbol = "peritus_process::tests::process_integration::native_backend::rejection::preparation_failure_is_durable_and_cannot_be_replayed", command = "cargo test --package peritus-process --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0135"
kind = "refinement"
statement = "Every registered C4 tool descriptor names the exact authenticated B1 operation, matches its operation class and mandatory risks, and declares a side-effect class no broader than that operation."
owning_crate = "peritus-tool-router"
source_file = "crates/tools/peritus-tool-router/src/verified.rs"
symbol = "peritus_tool_router::verified::tool_operation_refinement_complete"
status = "in-progress"
dependencies = []
live_issue = "#13"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/tools/peritus-tool-router/src/verified.rs", symbol = "peritus_tool_router::verified::tool_operation_refinement_complete", command = "cargo verus verify --package peritus-tool-router --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/tools/peritus-tool-router/src/verified.rs", symbol = "peritus_tool_router::verified::tests::operation_refinement_rejects_each_independent_mismatch", command = "cargo test --package peritus-tool-router --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0136"
kind = "refinement"
statement = "The C4 router alone constructs its private one-use invocation permit after the prepared descriptor, intent, lifecycle, capability, budget, conditional lease, committed dispatch, authority time, revision, and caller target all match; every rejected request has zero built-in-tool effects."
owning_crate = "peritus-tool-router"
source_file = "crates/tools/peritus-tool-router/src/verified.rs"
symbol = "peritus_tool_router::verified::tool_authority_complete"
status = "in-progress"
dependencies = ["OBL-0103", "OBL-0108", "OBL-0114", "OBL-0118", "OBL-0119"]
live_issue = "#13"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/tools/peritus-tool-router/src/verified.rs", symbol = "peritus_tool_router::verified::tool_authority_complete", command = "cargo verus verify --package peritus-tool-router --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/tools/peritus-tool-router/tests/router_authority.rs", symbol = "peritus_tool_router::tests::router_authority::authority_rejection_never_calls_dispatcher", command = "cargo test --package peritus-tool-router --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/tools/peritus-tool-router/tests/production_conformance.rs", symbol = "peritus_tool_router::tests::production_conformance::production_router_passes_all_eight_a2_tool_cases", command = "cargo test --package peritus-tool-router --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0137"
kind = "refinement"
statement = "Every checked C6 capability view contains only operation classes permitted by its exact canonical B1 actor role, so role-aware context presentation cannot widen authority or issue a capability."
owning_crate = "peritus-role"
source_file = "crates/orchestration/peritus-role/src/capability_view.rs"
symbol = "peritus_role::capability_view::CapabilityView::spec_is_narrow"
status = "in-progress"
dependencies = ["INV-006"]
live_issue = "#15"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-role/src/capability_view.rs", symbol = "peritus_role::capability_view::CapabilityView::spec_is_narrow", command = "cargo verus verify --package peritus-role --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-role/tests/role_matrix.rs", symbol = "peritus_role::tests::role_matrix::every_b1_role_has_an_explicit_non_widening_profile", command = "cargo test --package peritus-role --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-021"
kind = "invariant"
statement = "Every selected C6 memory candidate is structurally limited to the closed evidence-source set and mandatory quoted-evidence presentation; the memory boundary cannot represent policy, a capability, or an authority transition."
owning_crate = "peritus-memory"
source_file = "crates/orchestration/peritus-memory/src/retrieval/output.rs"
symbol = "peritus_memory::retrieval::output::MemoryCandidate::spec_is_quoted_evidence"
status = "in-progress"
dependencies = ["INV-006"]
live_issue = "#15"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-memory/src/retrieval/output.rs", symbol = "peritus_memory::retrieval::output::MemoryCandidate::spec_is_quoted_evidence", command = "cargo verus verify --package peritus-memory --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-memory/tests/poisoning.rs", symbol = "peritus_memory::tests::poisoning::instruction_like_payloads_retain_source_and_quote_boundary", command = "cargo test --package peritus-memory --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0138"
kind = "refinement"
statement = "Every C6 context provenance label enforces its exact authority ceiling, and repository, external, memory, tool, agent, review, and compacted content can carry only non-authoritative metadata regardless of its text."
owning_crate = "peritus-context"
source_file = "crates/orchestration/peritus-context/src/provenance.rs"
symbol = "peritus_context::provenance::Provenance::spec_permits_authority"
status = "in-progress"
dependencies = ["INV-006", "INV-021"]
live_issue = "#15"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-context/src/provenance.rs", symbol = "peritus_context::provenance::Provenance::spec_permits_authority", command = "cargo verus verify --package peritus-context --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-context/tests/poisoning.rs", symbol = "peritus_context::tests::poisoning::instruction_like_payloads_from_every_non_authoritative_source_stay_evidence", command = "cargo test --package peritus-context --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0139"
kind = "contract"
statement = "C6 compaction rejects system policy, application policy, immutable specifications, active user instructions, capability facts, and unresolved blocking findings as sources before constructing a derived node."
owning_crate = "peritus-context"
source_file = "crates/orchestration/peritus-context/src/content.rs"
symbol = "peritus_context::content::ContentKind::spec_is_compaction_protected"
status = "in-progress"
dependencies = ["OBL-0138"]
live_issue = "#15"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-context/src/content.rs", symbol = "peritus_context::content::ContentKind::spec_is_compaction_protected", command = "cargo verus verify --package peritus-context --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-context/tests/compaction_matrix.rs", symbol = "peritus_context::tests::compaction_matrix::protected_and_mixed_context_sources_are_rejected", command = "cargo test --package peritus-context --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0140"
kind = "invariant"
statement = "Every successful C6 token plan keeps output, protocol, and selected-input use within the context window and exactly conserves usable input as selected plus remaining input."
owning_crate = "peritus-context"
source_file = "crates/orchestration/peritus-context/src/budget.rs"
symbol = "peritus_context::budget::TokenAccounting::spec_is_bounded"
status = "in-progress"
dependencies = []
live_issue = "#15"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-context/src/budget.rs", symbol = "peritus_context::budget::TokenAccounting::spec_is_bounded", command = "cargo verus verify --package peritus-context --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-context/tests/selection_matrix.rs", symbol = "peritus_context::tests::selection_matrix::token_budget_checks_boundaries_and_reports_each_component", command = "cargo test --package peritus-context --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0141"
kind = "invariant"
statement = "A C6 memory tombstone suppresses exactly the same memory identity at every revision less than or equal to its last-known revision, while a genuinely newer revision remains eligible for replay."
owning_crate = "peritus-memory"
source_file = "crates/orchestration/peritus-memory/src/tombstone.rs"
symbol = "peritus_memory::tombstone::MemoryTombstone::spec_dominates"
status = "in-progress"
dependencies = ["INV-021"]
live_issue = "#15"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-memory/src/tombstone.rs", symbol = "peritus_memory::tombstone::MemoryTombstone::spec_dominates", command = "cargo verus verify --package peritus-memory --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-memory/tests/index_rebuild.rs", symbol = "peritus_memory::tests::index_rebuild::tombstone_dominates_records_at_or_below_its_revision", command = "cargo test --package peritus-memory --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0142"
kind = "invariant"
statement = "Every C6 retrieval plan admits selected memory only while its summed estimated tokens remain within the exact caller-supplied token budget."
owning_crate = "peritus-memory"
source_file = "crates/orchestration/peritus-memory/src/retrieval/output.rs"
symbol = "peritus_memory::retrieval::output::RetrievalPlan::spec_is_bounded"
status = "in-progress"
dependencies = ["INV-021"]
live_issue = "#15"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-memory/src/retrieval/output.rs", symbol = "peritus_memory::retrieval::output::RetrievalPlan::spec_is_bounded", command = "cargo verus verify --package peritus-memory --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-memory/tests/retrieval_matrix.rs", symbol = "peritus_memory::tests::retrieval_matrix::budget_and_result_limits_explain_every_unselected_candidate", command = "cargo test --package peritus-memory --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-010"
kind = "invariant"
statement = "Every current D2 finding remains open until the same current revision carries a reviewer-confirmed resolution, reviewer-confirmed invalidation, provenance-preserving confirmed supersession, or an exact externally authorized waiver observation; no fixer claim or historical record can remove it implicitly."
owning_crate = "peritus-review"
source_file = "crates/orchestration/peritus-review/src/verified/executable.rs"
symbol = "peritus_review::verified::executable::findings_are_conserved"
status = "in-progress"
dependencies = ["INV-003", "INV-004"]
live_issue = "#18"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-review/src/verified.rs", symbol = "peritus_review::verified::missing_conservation_cannot_complete", command = "cargo verus verify --package peritus-review --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-review/tests/domain_engine.rs", symbol = "peritus_review::tests::domain_engine::fixer_evidence_needs_independent_current_reviewer_confirmation", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-review/tests/domain_transitions.rs", symbol = "peritus_review::tests::domain_transitions::reconciliation_retains_all_sources_evidence_and_history", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0143"
kind = "refinement"
statement = "D2 treats review, finding, disposition, and waiver evidence as current only when every exact RevisionTuple component and the candidate/tree binding match; advancing the binding retains history but removes stale material from quorum, conservation, and completion."
owning_crate = "peritus-review"
source_file = "crates/orchestration/peritus-review/src/verified.rs"
symbol = "peritus_review::verified::exact_freshness"
status = "in-progress"
dependencies = ["INV-003"]
live_issue = "#18"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-review/src/verified.rs", symbol = "peritus_review::verified::stale_evidence_is_not_fresh", command = "cargo verus verify --package peritus-review --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-review/tests/domain_engine.rs", symbol = "peritus_review::tests::domain_engine::external_waiver_is_consumed_exactly_and_revision_advance_resets_current_truth", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0144"
kind = "invariant"
statement = "D2 review completion requires every named quorum dimension independently: submitted count, category coverage, distinct reviewers, producer independence, distinct contexts, model families, providers, no shared ancestry, and fresh C6 context."
owning_crate = "peritus-review"
source_file = "crates/orchestration/peritus-review/src/verified.rs"
symbol = "peritus_review::verified::complete_quorum"
status = "in-progress"
dependencies = ["INV-004", "OBL-0138"]
live_issue = "#18"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-review/src/verified.rs", symbol = "peritus_review::verified::missing_fresh_context_breaks_quorum", command = "cargo verus verify --package peritus-review --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-review/tests/domain_engine.rs", symbol = "peritus_review::tests::domain_engine::independent_quorum_dimensions_and_truthful_completion", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-review/tests/domain_engine.rs", symbol = "peritus_review::tests::domain_engine::each_independence_fact_stays_visible_in_the_report", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0145"
kind = "invariant"
statement = "D2 can report Completed only with complete current quorum, complete finding conservation, and no oscillation trigger; cancellation, failure, budget/cycle exhaustion, stale or malformed evidence, and non-progress remain explicit non-success."
owning_crate = "peritus-review"
source_file = "crates/orchestration/peritus-review/src/verified/executable.rs"
symbol = "peritus_review::verified::executable::no_implicit_success"
status = "in-progress"
dependencies = ["INV-005", "INV-010", "OBL-0144"]
live_issue = "#18"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-review/src/verified.rs", symbol = "peritus_review::verified::missing_quorum_cannot_complete", command = "cargo verus verify --package peritus-review --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-review/src/verified.rs", symbol = "peritus_review::verified::bounded_cycle_successor", command = "cargo verus verify --package peritus-review --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-review/tests/domain_engine.rs", symbol = "peritus_review::tests::domain_engine::cancellation_budget_exhaustion_and_failure_never_become_completion", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-review/tests/domain_transitions.rs", symbol = "peritus_review::tests::domain_transitions::repeated_fingerprint_and_cycle_exhaustion_finalize_needs_human", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0146"
kind = "refinement"
statement = "Every durable D2 state is reconstructed by applying the closed reducer to the exact contiguous C0 event chain from genesis, and startup accepts a family-55 checkpoint only when its complete canonical state equals semantic replay."
owning_crate = "peritus-review"
source_file = "crates/orchestration/peritus-review/src/verified/executable.rs"
symbol = "peritus_review::verified::executable::replay_equivalent"
status = "in-progress"
dependencies = ["OBL-0115", "OBL-0143", "OBL-0145"]
live_issue = "#18"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-review/src/verified.rs", symbol = "peritus_review::verified::replay_is_reflexive", command = "cargo verus verify --package peritus-review --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-review/tests/durability_restart.rs", symbol = "peritus_review::tests::durability_restart::durability_restart_idempotency_conflict_and_checkpoint_corruption", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-review/tests/domain_engine.rs", symbol = "peritus_review::tests::domain_engine::independent_quorum_dimensions_and_truthful_completion", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0147"
kind = "contract"
statement = "The closed D2 command, event, and state wire families have byte-exact checked-in version-one fixtures; their canonical decoders reject unknown closed tags and trailing bytes instead of accepting an ambiguous representation."
owning_crate = "peritus-review"
source_file = "crates/orchestration/peritus-review/src/wire/fixture_tests.rs"
symbol = "peritus_review::wire::fixture_tests::canonical_binary_corpus_is_exact_and_rejects_closed_tag_or_trailing_corruption"
status = "in-progress"
dependencies = ["OBL-0146"]
live_issue = "#18"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-review/src/wire/fixture_tests.rs", symbol = "peritus_review::wire::fixture_tests::canonical_binary_corpus_is_exact_and_rejects_closed_tag_or_trailing_corruption", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0148"
kind = "invariant"
statement = "D2 accepts only exact bounded bindings, authority-correct finding-disposition transitions, and single-event reducer successors with current sequence, predecessor, revision, digest, and command-identity fences; any deterministic repeat, stagnation, regression, disagreement, or cycle exhaustion input requires escalation."
owning_crate = "peritus-review"
source_file = "crates/orchestration/peritus-review/src/verified.rs"
symbol = "peritus_review::verified::legal_reducer_step"
status = "in-progress"
dependencies = ["INV-010", "OBL-0143", "OBL-0145"]
live_issue = "#18"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-review/src/verified.rs", symbol = "peritus_review::verified::stale_sequence_rejects_reducer_step", command = "cargo verus verify --package peritus-review --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-review/src/verified.rs", symbol = "peritus_review::verified::reviewer_resolution_requires_confirmation", command = "cargo verus verify --package peritus-review --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-review/tests/domain_transitions.rs", symbol = "peritus_review::tests::domain_transitions::repeated_fingerprint_and_cycle_exhaustion_finalize_needs_human", command = "cargo test --package peritus-review --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0149"
kind = "invariant"
statement = "D3 scheduler reservations preserve every global and worker resource capacity, each live dispatch has one work and worker owner, and checked release cannot create or lose capacity."
owning_crate = "peritus-scheduler"
source_file = "crates/orchestration/peritus-scheduler/src/verified.rs"
symbol = "peritus_scheduler::verified::reservations_fit"
status = "in-progress"
dependencies = ["INV-003", "INV-005"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-scheduler/src/verified.rs", symbol = "peritus_scheduler::verified::conservation_preserves_capacity", command = "cargo verus verify --package peritus-scheduler --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-scheduler/tests/resource_invariants.rs", symbol = "peritus_scheduler::tests::resource_invariants::resource_addition_and_subtraction_are_exact_and_checked", command = "cargo test --package peritus-scheduler --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0150"
kind = "refinement"
statement = "D3 scheduler readiness requires successful dependencies and deterministic feasible selection uses bounded bypass, priority, enqueue order, and identity without wall time or runtime ordering."
owning_crate = "peritus-scheduler"
source_file = "crates/orchestration/peritus-scheduler/src/selection.rs"
symbol = "peritus_scheduler::selection::select_next"
status = "in-progress"
dependencies = ["OBL-0149"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-scheduler/src/verified.rs", symbol = "peritus_scheduler::verified::saturated_bypass_stays_bounded", command = "cargo verus verify --package peritus-scheduler --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-scheduler/src/verified.rs", symbol = "peritus_scheduler::verified::missing_dependency_success_blocks", command = "cargo verus verify --package peritus-scheduler --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-scheduler/tests/selection_fairness.rs", symbol = "peritus_scheduler::tests::selection_fairness::implementation_matches_independent_reference_and_forces_aged_work", command = "cargo test --package peritus-scheduler --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0151"
kind = "invariant"
statement = "D3 scheduler completion requires every admitted work item to succeed with no live reservation or pending directive; cancellation, failure, dependency failure, ambiguity, and exhaustion remain non-success."
owning_crate = "peritus-scheduler"
source_file = "crates/orchestration/peritus-scheduler/src/verified.rs"
symbol = "peritus_scheduler::verified::no_implicit_success"
status = "in-progress"
dependencies = ["INV-005", "OBL-0149", "OBL-0150"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-scheduler/src/verified.rs", symbol = "peritus_scheduler::verified::completion_requires_quiescence", command = "cargo verus verify --package peritus-scheduler --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-scheduler/tests/domain_transitions.rs", symbol = "peritus_scheduler::tests::domain_transitions::pause_drain_and_terminal_truth_are_closed_and_explicit", command = "cargo test --package peritus-scheduler --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0152"
kind = "refinement"
statement = "Every durable D3 scheduler state is reconstructed from the exact contiguous event chain and a family-72 checkpoint is accepted only when complete canonical state equals semantic replay."
owning_crate = "peritus-scheduler"
source_file = "crates/orchestration/peritus-scheduler/src/verified.rs"
symbol = "peritus_scheduler::verified::replay_equivalent"
status = "in-progress"
dependencies = ["OBL-0115", "OBL-0151"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-scheduler/tests/durability_restart.rs", symbol = "peritus_scheduler::tests::durability_restart::commit_restart_resolution_and_conflicting_command_are_exact", command = "cargo test --package peritus-scheduler --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0153"
kind = "invariant"
statement = "Every D3 collaboration task has one acyclic depth-consistent parent chain to its root, and a successful task satisfies its exact all-required or any-required child join."
owning_crate = "peritus-collaboration"
source_file = "crates/orchestration/peritus-collaboration/src/verified/executable.rs"
symbol = "peritus_collaboration::verified::executable::causal_graph_is_valid"
status = "in-progress"
dependencies = ["INV-002", "OBL-0149"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-collaboration/src/verified.rs", symbol = "peritus_collaboration::verified::child_requires_parent", command = "cargo verus verify --package peritus-collaboration --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-collaboration/src/verified.rs", symbol = "peritus_collaboration::verified::all_join_rejects_missing", command = "cargo verus verify --package peritus-collaboration --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-collaboration/tests/domain_lifecycle.rs", symbol = "peritus_collaboration::tests::domain_lifecycle::all_required_lifecycle_message_handoff_and_finalization_are_exact", command = "cargo test --package peritus-collaboration --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0154"
kind = "invariant"
statement = "D3 collaboration cancellation dominates every descendant and later success, while aggregate completion requires a successful root, truthful joins, terminal tasks, and no pending delivery or cancellation."
owning_crate = "peritus-collaboration"
source_file = "crates/orchestration/peritus-collaboration/src/verified/executable.rs"
symbol = "peritus_collaboration::verified::executable::cancellation_dominates"
status = "in-progress"
dependencies = ["INV-005", "OBL-0153"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-collaboration/src/verified.rs", symbol = "peritus_collaboration::verified::cancellation_excludes_success", command = "cargo verus verify --package peritus-collaboration --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-collaboration/src/verified.rs", symbol = "peritus_collaboration::verified::pending_directive_blocks_completion", command = "cargo verus verify --package peritus-collaboration --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-collaboration/tests/cancellation_causality.rs", symbol = "peritus_collaboration::tests::cancellation_causality::cancellation_propagates_canonically_and_cannot_resurrect_success", command = "cargo test --package peritus-collaboration --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0155"
kind = "refinement"
statement = "Every durable D3 collaboration state is reconstructed through its closed reducer from a contiguous causal event chain, and family-75 checkpoint bytes must equal semantic replay."
owning_crate = "peritus-collaboration"
source_file = "crates/orchestration/peritus-collaboration/src/verified/executable.rs"
symbol = "peritus_collaboration::verified::executable::replay_equivalent"
status = "in-progress"
dependencies = ["OBL-0115", "OBL-0153", "OBL-0154"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-collaboration/src/verified.rs", symbol = "peritus_collaboration::verified::replay_claim_requires_equality", command = "cargo verus verify --package peritus-collaboration --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-collaboration/tests/durability_restart.rs", symbol = "peritus_collaboration::tests::durability_restart::exact_transition_survives_restart_and_resolves_idempotently", command = "cargo test --package peritus-collaboration --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0156"
kind = "invariant"
statement = "E0 keeps service, writer, fixer, and reviewer authority separated, and every installed candidate cycle binds changed material to the exact revision without reusing any historical D1 or D3 aggregate identity."
owning_crate = "peritus-orchestrator"
source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs"
symbol = "peritus_orchestrator::verified::roles_are_separated"
status = "in-progress"
dependencies = ["INV-003", "INV-004", "OBL-0153"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs", symbol = "peritus_orchestrator::verified::writer_fixer_alias_breaks_separation", command = "cargo verus verify --package peritus-orchestrator --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs", symbol = "peritus_orchestrator::verified::reused_child_run_breaks_freshness", command = "cargo verus verify --package peritus-orchestrator --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-orchestrator/tests/domain_quality_cycle.rs", symbol = "peritus_orchestrator::tests::domain_quality_cycle::cycle_validation_is_typed_to_the_current_candidate_revision", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/integration_tests/lifecycle.rs", symbol = "peritus_orchestrator::integration_tests::lifecycle::blocking_finding_fixer_revision_and_fresh_quality_cycle_reach_acceptance", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0157"
kind = "invariant"
statement = "Every accepted E0 reducer step consumes one exact run, revision, sequence, predecessor, state-digest, and fresh command fence, emits exactly one event, and keeps each independent work and retention counter within its immutable bound."
owning_crate = "peritus-orchestrator"
source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs"
symbol = "peritus_orchestrator::verified::transition_is_legal"
status = "in-progress"
dependencies = ["INV-005", "OBL-0115", "OBL-0156"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs", symbol = "peritus_orchestrator::verified::counter_over_limit_is_rejected", command = "cargo verus verify --package peritus-orchestrator --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs", symbol = "peritus_orchestrator::verified::reducer_step_emits_exactly_one_event", command = "cargo verus verify --package peritus-orchestrator --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-orchestrator/tests/domain_identity_limits.rs", symbol = "peritus_orchestrator::tests::domain_identity_limits::every_limit_rejects_zero_or_compiled_ceiling_excess", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/integration_tests/lifecycle.rs", symbol = "peritus_orchestrator::integration_tests::lifecycle::writer_gates_review_b2_and_durable_b0_are_the_only_success_path", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0158"
kind = "invariant"
statement = "E0 Accepted is quiescent and requires an exact current B2 certificate plus both planned durable B0 Begin and Evaluate observations; no certificate, request, local decision, stale child result, or missing stage can imply acceptance."
owning_crate = "peritus-orchestrator"
source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs"
symbol = "peritus_orchestrator::verified::terminal_is_truthful"
status = "in-progress"
dependencies = ["INV-005", "OBL-0145", "OBL-0157"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs", symbol = "peritus_orchestrator::verified::certificate_alone_cannot_accept", command = "cargo verus verify --package peritus-orchestrator --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs", symbol = "peritus_orchestrator::verified::evaluation_without_begin_cannot_accept", command = "cargo verus verify --package peritus-orchestrator --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/wire/fixture_tests/accepted.rs", symbol = "peritus_orchestrator::wire::fixture_tests::accepted::accepted_checkpoint_requires_planned_begin_and_evaluate_observations", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/integration_tests/lifecycle.rs", symbol = "peritus_orchestrator::integration_tests::lifecycle::writer_gates_review_b2_and_durable_b0_are_the_only_success_path", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0159"
kind = "invariant"
statement = "Once E0 enters cancellation or non-success settlement, late success cannot win; every owned child must become terminal or retain an exact evidence-backed unreachable or ambiguous classification before finalization."
owning_crate = "peritus-orchestrator"
source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs"
symbol = "peritus_orchestrator::verified::cancellation_dominates"
status = "in-progress"
dependencies = ["INV-005", "OBL-0154", "OBL-0158"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs", symbol = "peritus_orchestrator::verified::cancelled_run_cannot_accept_late_success", command = "cargo verus verify --package peritus-orchestrator --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs", symbol = "peritus_orchestrator::verified::live_child_blocks_terminal", command = "cargo verus verify --package peritus-orchestrator --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/integration_tests/cancellation.rs", symbol = "peritus_orchestrator::integration_tests::cancellation::cancellation_settles_owned_children_and_late_success_cannot_win", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/integration_tests/cancellation.rs", symbol = "peritus_orchestrator::integration_tests::cancellation::ambiguous_child_settlement_truthfully_requires_human_judgment", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0160"
kind = "refinement"
statement = "Every durable E0 state is reconstructed from its exact contiguous family-77 event chain, and a family-78 checkpoint is accepted only when its complete semantic state equals replay; committed directives remain commit-before-effect and idempotent across restart."
owning_crate = "peritus-orchestrator"
source_file = "crates/orchestration/peritus-orchestrator/src/replay.rs"
symbol = "peritus_orchestrator::replay::OrchestratorReplay::rebuild"
status = "in-progress"
dependencies = ["OBL-0115", "OBL-0157", "OBL-0159"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-orchestrator/src/verified.rs", symbol = "peritus_orchestrator::verified::replay_claim_requires_equality", command = "cargo verus verify --package peritus-orchestrator --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/integration_tests/durability.rs", symbol = "peritus_orchestrator::integration_tests::durability::every_lifecycle_commit_restarts_to_the_exact_checkpoint", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/integration_tests/crash_matrix.rs", symbol = "peritus_orchestrator::integration_tests::crash_matrix::crash_after_commit_before_publish_recovers_the_same_deliverable_directive", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0161"
kind = "contract"
statement = "The closed E0 command, event, and complete-state wire families 76, 77, and 78 have byte-exact immutable version-one fixtures; decoders reject unknown closed tags, trailing bytes, stale D1 snapshots, stale D2 bindings, and accepted checkpoints missing exact B0 stages."
owning_crate = "peritus-orchestrator"
source_file = "crates/orchestration/peritus-orchestrator/src/wire/fixture_tests.rs"
symbol = "peritus_orchestrator::wire::fixture_tests::canonical_corpus_is_exact_and_rejects_corruption"
status = "in-progress"
dependencies = ["OBL-0158", "OBL-0160"]
live_issue = "#19"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/wire/fixture_tests.rs", symbol = "peritus_orchestrator::wire::fixture_tests::canonical_corpus_is_exact_and_rejects_corruption", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/wire/fixture_tests/observation_binding.rs", symbol = "peritus_orchestrator::wire::fixture_tests::observation_binding::checkpoint_rejects_stale_gate_snapshot_and_review_binding", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-orchestrator/src/integration_tests/lifecycle.rs", symbol = "peritus_orchestrator::integration_tests::lifecycle::one_field_contract_candidate_artifact_and_child_binding_drift_is_rejected", command = "cargo test --package peritus-orchestrator --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-017"
kind = "invariant"
statement = "An E1 successor preserves the complete ordered identity and content of every protected controlled asset; additions, removals, reordering, ownership changes, authority changes, path changes, and byte changes are rejected."
owning_crate = "peritus-harness"
source_file = "crates/orchestration/peritus-harness/src/domain/verified.rs"
symbol = "peritus_harness::domain::verified::protected_assets_are_invariant"
status = "in-progress"
dependencies = ["INV-003", "OBL-0156"]
live_issue = "#20"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-harness/src/domain/verified.rs", symbol = "peritus_harness::domain::verified::protected_invariance_reflexive", command = "cargo verus verify --package peritus-harness --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-harness/tests/domain_revision.rs", symbol = "peritus_harness::tests::domain_revision::every_structural_change_to_a_protected_asset_rejects", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-harness/tests/domain_revision.rs", symbol = "peritus_harness::tests::domain_revision::protected_asset_addition_removal_and_reordering_reject", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0162"
kind = "invariant"
statement = "Every checked E1 component graph has unique component identities, a complete deterministic dependency-first order, exact kind/schema/digest/feature compatibility, and declared plus transitive authority within compiled ceilings."
owning_crate = "peritus-harness"
source_file = "crates/orchestration/peritus-harness/src/domain/verified.rs"
symbol = "peritus_harness::domain::verified::topological_order_is_complete"
status = "in-progress"
dependencies = ["INV-003", "INV-017"]
live_issue = "#20"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-harness/src/domain/verified.rs", symbol = "peritus_harness::domain::verified::earlier_dependency_is_legal", command = "cargo verus verify --package peritus-harness --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-harness/tests/domain_graph.rs", symbol = "peritus_harness::tests::domain_graph::transitive_authority_and_incompatible_protected_dependencies_reject", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-harness/tests/domain_properties.rs", symbol = "peritus_harness::tests::domain_properties::all_small_ranked_dags_match_the_reference_checker_and_round_trip", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0163"
kind = "invariant"
statement = "E1 graph and revision identities bind all canonical bytes, history retains an exact immutable prefix, and rollback can select only a strict retained ancestor while creating new materialization evidence."
owning_crate = "peritus-harness"
source_file = "crates/orchestration/peritus-harness/src/domain/verified.rs"
symbol = "peritus_harness::domain::verified::graph_digest_is_bound"
status = "in-progress"
dependencies = ["INV-017", "OBL-0162"]
live_issue = "#20"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-harness/src/domain/verified.rs", symbol = "peritus_harness::domain::verified::digest_binding_reflexive", command = "cargo verus verify --package peritus-harness --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "verus-proof", source_file = "crates/orchestration/peritus-harness/src/domain/verified.rs", symbol = "peritus_harness::domain::verified::direct_predecessor_is_ancestor", command = "cargo verus verify --package peritus-harness --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "property-test", source_file = "crates/orchestration/peritus-harness/tests/domain_revision.rs", symbol = "peritus_harness::tests::domain_revision::history_retains_branches_and_allows_only_strict_ancestor_rollback", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0164"
kind = "contract"
statement = "A strict E1 manifest and recursive inventory activate only checked bounded component content, and each deterministic C1 plan is confined to the exact owned target set with artifact, preimage, revision, snapshot, and rollback-reason bindings."
owning_crate = "peritus-harness"
source_file = "crates/orchestration/peritus-harness/src/materialization/planner.rs"
symbol = "peritus_harness::materialization::planner::MaterializationPlan::build"
status = "in-progress"
dependencies = ["INV-017", "OBL-0162", "OBL-0163"]
live_issue = "#20"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-harness/tests/manifest_document.rs", symbol = "peritus_harness::tests::manifest_document::strict_manifest_retains_exact_bytes_and_checked_declaration", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-harness/tests/materialization_safety.rs", symbol = "peritus_harness::tests::materialization_safety::deterministic_plan_round_trips_exact_artifact_and_snapshot_bindings", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0165"
kind = "refinement"
statement = "Every accepted E1 transition, complete checkpoint, artifact dependency, and stable materialization directive commits atomically in C0; exact retries resolve idempotently, restart replay reconstructs the same state, and settlement acknowledges only the exact claimed outbox fence in the same append transaction."
owning_crate = "peritus-harness"
source_file = "crates/orchestration/peritus-harness/src/durability/commit.rs"
symbol = "peritus_harness::durability::commit::commit_harness_settlement"
status = "in-progress"
dependencies = ["OBL-0115", "OBL-0163", "OBL-0164"]
live_issue = "#20"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-harness/tests/durability_restart.rs", symbol = "peritus_harness::tests::durability_restart::atomic_genesis_commit_is_idempotent_and_restart_replay_matches_checkpoint", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-harness/tests/durability_restart.rs", symbol = "peritus_harness::tests::durability_restart::checked_restart_recreates_plan_and_atomically_settles_exact_claim", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0166"
kind = "contract"
statement = "The closed E1 command, event, and complete-state wire families 79, 80, and 81 have byte-exact immutable version-one fixtures; checked decoders reject wrong families, unknown closed command tags, malformed semantic data, and trailing bytes."
owning_crate = "peritus-harness"
source_file = "crates/orchestration/peritus-harness/tests/replay_wire.rs"
symbol = "peritus_harness::tests::replay_wire::canonical_corpus_is_exact_and_rejects_corruption"
status = "in-progress"
dependencies = ["OBL-0163", "OBL-0165"]
live_issue = "#20"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-harness/tests/replay_wire.rs", symbol = "peritus_harness::tests::replay_wire::canonical_corpus_is_exact_and_rejects_corruption", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-harness/tests/replay_wire.rs", symbol = "peritus_harness::tests::replay_wire::replay_projection_matches_live_state_and_rejects_duplicate_genesis", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0167"
kind = "contract"
statement = "E0 and later consumers retain the unchanged seven-field shared RevisionTuple while a checked governing binding additionally carries the exact branch-distinguishing E1 revision identity and matching installed materialization receipt."
owning_crate = "peritus-harness"
source_file = "crates/orchestration/peritus-harness/src/runtime/binding.rs"
symbol = "peritus_harness::runtime::binding::GoverningHarnessBinding::new"
status = "in-progress"
dependencies = ["OBL-0156", "OBL-0164", "OBL-0165"]
live_issue = "#20"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/orchestration/peritus-harness/src/runtime/binding.rs", symbol = "peritus_harness::runtime::binding::tests::exact_binding_retains_the_shared_tuple_and_rejects_drift", command = "cargo test --package peritus-harness --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0173"
kind = "invariant"
statement = "Every E2 selection-manifest entry belongs to exactly one frozen diagnostic subject and one matching C0-backed C7 event; causal closure adds only valid same-subject ancestors and the complete selection remains within its frozen bounds."
owning_crate = "peritus-debugger"
source_file = "crates/analysis/peritus-debugger/src/verified.rs"
symbol = "peritus_debugger::verified::selection_containment"
status = "in-progress"
dependencies = ["OBL-0115", "OBL-0167"]
live_issue = "#21"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-debugger/src/verified.rs", symbol = "peritus_debugger::verified::selection_containment", command = "cargo verus verify --package peritus-debugger --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-debugger/tests/domain_primitives.rs", symbol = "peritus_debugger::tests::domain_primitives::refinement_selection_containment", command = "cargo test --package peritus-debugger --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0168"
kind = "invariant"
statement = "Every validated E2 citation names one selected event with exact subject, journal position, and frame identity; any artifact citation names a selected finalized ordinary artifact and a nonempty in-range byte span."
owning_crate = "peritus-debugger"
source_file = "crates/analysis/peritus-debugger/src/verified.rs"
symbol = "peritus_debugger::verified::citation_containment"
status = "in-progress"
dependencies = ["OBL-0173"]
live_issue = "#21"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-debugger/src/verified.rs", symbol = "peritus_debugger::verified::citation_containment", command = "cargo verus verify --package peritus-debugger --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-debugger/tests/domain_primitives.rs", symbol = "peritus_debugger::tests::domain_primitives::refinement_citation_containment", command = "cargo test --package peritus-debugger --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0169"
kind = "invariant"
statement = "Every validated E2 report is subject-bound, canonically ordered, bounded, taxonomy-complete, citation-valid, uncertainty-preserving, and diagnostic-only before canonical encoding, artifact finalization, or evidence admission."
owning_crate = "peritus-debugger"
source_file = "crates/analysis/peritus-debugger/src/verified.rs"
symbol = "peritus_debugger::verified::report_validity"
status = "in-progress"
dependencies = ["OBL-0173", "OBL-0168"]
live_issue = "#21"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-debugger/src/verified.rs", symbol = "peritus_debugger::verified::report_validity", command = "cargo verus verify --package peritus-debugger --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-debugger/tests/domain_primitives.rs", symbol = "peritus_debugger::tests::domain_primitives::refinement_report_validity", command = "cargo test --package peritus-debugger --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0170"
kind = "refinement"
statement = "Applying the complete legal E2 event prefix through the pure reducer produces the same state and digest as the accepted complete checkpoint, and cancellation or another terminal state cannot transition to later success."
owning_crate = "peritus-debugger"
source_file = "crates/analysis/peritus-debugger/src/verified.rs"
symbol = "peritus_debugger::verified::replay_equivalence"
status = "in-progress"
dependencies = ["OBL-0115", "OBL-0173"]
live_issue = "#21"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-debugger/src/verified.rs", symbol = "peritus_debugger::verified::replay_equivalence", command = "cargo verus verify --package peritus-debugger --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-debugger/tests/domain_primitives.rs", symbol = "peritus_debugger::tests::domain_primitives::refinement_replay_equivalence", command = "cargo test --package peritus-debugger --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0171"
kind = "invariant"
statement = "Every E2 selection, timeline, cause, alternative, contrary citation, cluster, pattern member, component link, claim, diagnostic, model stream, report, event, and complete state remains within the independently frozen job ceilings."
owning_crate = "peritus-debugger"
source_file = "crates/analysis/peritus-debugger/src/verified.rs"
symbol = "peritus_debugger::verified::bounded_analysis"
status = "in-progress"
dependencies = ["OBL-0173", "OBL-0169", "OBL-0170"]
live_issue = "#21"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-debugger/src/verified.rs", symbol = "peritus_debugger::verified::bounded_analysis", command = "cargo verus verify --package peritus-debugger --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-debugger/tests/domain_primitives.rs", symbol = "peritus_debugger::tests::domain_primitives::refinement_bounded_analysis", command = "cargo test --package peritus-debugger --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0172"
kind = "invariant"
statement = "Checked E2 reports and model proposals leave source evidence unchanged and cannot represent E1 mutation, acceptance, waiver, evaluation, promotion, production activation, rollback authority, a production pointer, or a capability."
owning_crate = "peritus-debugger"
source_file = "crates/analysis/peritus-debugger/src/verified.rs"
symbol = "peritus_debugger::verified::non_authority"
status = "in-progress"
dependencies = ["INV-003", "INV-004", "OBL-0169"]
live_issue = "#21"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-debugger/src/verified.rs", symbol = "peritus_debugger::verified::non_authority", command = "cargo verus verify --package peritus-debugger --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-debugger/tests/domain_primitives.rs", symbol = "peritus_debugger::tests::domain_primitives::refinement_non_authority", command = "cargo test --package peritus-debugger --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0174"
kind = "invariant"
statement = "Every frozen E3 profile binds the exact dataset and partition identities, provider snapshot, model controls, baseline and candidate E1 revisions, execution policies, evaluator policy, and independent limits before planning or execution."
owning_crate = "peritus-eval"
source_file = "crates/analysis/peritus-eval/src/verified.rs"
symbol = "peritus_eval::verified::frozen_profile_spec"
status = "in-progress"
dependencies = ["OBL-0167"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-eval/src/verified.rs", symbol = "peritus_eval::verified::frozen_profile_spec", command = "cargo verus verify --package peritus-eval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-eval/tests/verified_refinement.rs", symbol = "peritus_eval::tests::verified_refinement::frozen_ledger_transition_replay_and_non_authority_roots_are_exact_conjunctions", command = "cargo test --package peritus-eval --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0175"
kind = "invariant"
statement = "Every E3 campaign conserves a duplicate-free planned rollout ledger into exactly one bounded terminal classification per rollout with monotonic attempts and complete paired-arm accounting."
owning_crate = "peritus-eval"
source_file = "crates/analysis/peritus-eval/src/verified.rs"
symbol = "peritus_eval::verified::ledger_validity_spec"
status = "in-progress"
dependencies = ["OBL-0174"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-eval/src/verified.rs", symbol = "peritus_eval::verified::ledger_validity_spec", command = "cargo verus verify --package peritus-eval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-eval/tests/verified_refinement.rs", symbol = "peritus_eval::tests::verified_refinement::frozen_ledger_transition_replay_and_non_authority_roots_are_exact_conjunctions", command = "cargo test --package peritus-eval --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0176"
kind = "invariant"
statement = "E3 statistical outputs use only checked integer and fixed-point inputs with valid pass-at-k preconditions, bounded ordered intervals, complete task pairing, deterministic bootstrap seeds, and frozen infrastructure exclusion policy."
owning_crate = "peritus-eval"
source_file = "crates/analysis/peritus-eval/src/verified.rs"
symbol = "peritus_eval::verified::statistical_validity_spec"
status = "in-progress"
dependencies = ["OBL-0175"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-eval/src/verified.rs", symbol = "peritus_eval::verified::statistical_validity_spec", command = "cargo verus verify --package peritus-eval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-eval/tests/verified_refinement.rs", symbol = "peritus_eval::tests::verified_refinement::accounting_and_statistical_preconditions_match_runtime_boundaries", command = "cargo test --package peritus-eval --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0177"
kind = "invariant"
statement = "Every accepted E3 transition has an exact command fence, legal campaign and rollout edge, finalized artifact closure, matching durable directive, and terminal dominance including cancellation."
owning_crate = "peritus-eval"
source_file = "crates/analysis/peritus-eval/src/verified.rs"
symbol = "peritus_eval::verified::transition_validity_spec"
status = "in-progress"
dependencies = ["OBL-0115", "OBL-0175"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-eval/src/verified.rs", symbol = "peritus_eval::verified::transition_validity_spec", command = "cargo verus verify --package peritus-eval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-eval/tests/cancellation.rs", symbol = "peritus_eval::tests::cancellation::cancellation_waits_for_each_external_rollout_before_terminal_completion", command = "cargo test --package peritus-eval --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0178"
kind = "refinement"
statement = "Replaying the complete contiguous E3 event prefix produces the exact checkpoint state and digest, while exact command retry and commit-before-effect directives cannot duplicate execution or publication."
owning_crate = "peritus-eval"
source_file = "crates/analysis/peritus-eval/src/verified.rs"
symbol = "peritus_eval::verified::replay_refinement_spec"
status = "in-progress"
dependencies = ["OBL-0115", "OBL-0177"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-eval/src/verified.rs", symbol = "peritus_eval::verified::replay_refinement_spec", command = "cargo verus verify --package peritus-eval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-eval/tests/durability_restart.rs", symbol = "peritus_eval::tests::durability_restart::genesis_commit_is_idempotent_and_restart_replay_matches_checkpoint", command = "cargo test --package peritus-eval --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0179"
kind = "contract"
statement = "The closed E3 command, event, and complete-state wire families 85, 86, and 87 have immutable schema-one fixtures and checked decoders that reject wrong families, malformed semantics, unknown tags, noncanonical values, truncation, and trailing bytes."
owning_crate = "peritus-eval"
source_file = "crates/analysis/peritus-eval/tests/replay_wire.rs"
symbol = "peritus_eval::tests::replay_wire::frozen_wire_corpus_is_byte_exact_and_malformed_frames_reject"
status = "in-progress"
dependencies = ["OBL-0178"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/analysis/peritus-eval/tests/replay_wire.rs", symbol = "peritus_eval::tests::replay_wire::frozen_wire_corpus_is_byte_exact_and_malformed_frames_reject", command = "cargo test --package peritus-eval --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0180"
kind = "invariant"
statement = "E3 reports retain source evidence unchanged and cannot represent harness mutation, acceptance, promotion, deployment, a production pointer, rollback authority, or an executable capability."
owning_crate = "peritus-eval"
source_file = "crates/analysis/peritus-eval/src/verified.rs"
symbol = "peritus_eval::verified::non_authority_spec"
status = "in-progress"
dependencies = ["OBL-0174", "OBL-0176", "OBL-0178"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-eval/src/verified.rs", symbol = "peritus_eval::verified::non_authority_spec", command = "cargo verus verify --package peritus-eval --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-eval/tests/verified_refinement.rs", symbol = "peritus_eval::tests::verified_refinement::frozen_ledger_transition_replay_and_non_authority_roots_are_exact_conjunctions", command = "cargo test --package peritus-eval --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-018"
kind = "invariant"
statement = "Every F0 candidate evaluation binds the exact E1 baseline and candidate, frozen E3 dataset/profile, unchanged sealed evaluator and protected promotion assets, and a complete declared change set before attribution or selection."
owning_crate = "peritus-evolution"
source_file = "crates/analysis/peritus-evolution/src/verified.rs"
symbol = "peritus_evolution::verified::evaluator_isolation"
status = "in-progress"
dependencies = ["OBL-0167", "OBL-0174", "OBL-0176"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-evolution/src/verified.rs", symbol = "peritus_evolution::verified::evaluator_isolation", command = "cargo verus verify --package peritus-evolution --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-evolution/tests/verified_refinement.rs", symbol = "peritus_evolution::tests::verified_refinement::evaluator_isolation_requires_every_checked_boundary_fact", command = "cargo test --package peritus-evolution --all-targets --all-features --locked" },
]

[[entries]]
id = "INV-019"
kind = "invariant"
statement = "F0 production promotion is legal only when immutable evidence is complete, the production baseline is current, attribution and deny-wins selection pass, evaluator isolation and schema compatibility hold, required review is complete, and exact fresh action and human authority are consumed."
owning_crate = "peritus-evolution"
source_file = "crates/analysis/peritus-evolution/src/verified.rs"
symbol = "peritus_evolution::verified::promotion_safety"
status = "in-progress"
dependencies = ["INV-018", "OBL-0181", "OBL-0185"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-evolution/src/verified.rs", symbol = "peritus_evolution::verified::promotion_safety", command = "cargo verus verify --package peritus-evolution --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-evolution/tests/verified_refinement.rs", symbol = "peritus_evolution::tests::verified_refinement::promotion_safety_is_a_conjunction_not_a_score", command = "cargo test --package peritus-evolution --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0181"
kind = "invariant"
statement = "F0 mandatory evaluation criteria are independently classified and deny-wins: incomplete, failed, or unavailable correctness, regression, safety, reliability, cost, latency, trace, teardown, attribution, compatibility, or review evidence cannot become an eligible selection."
owning_crate = "peritus-evolution"
source_file = "crates/analysis/peritus-evolution/src/verified.rs"
symbol = "peritus_evolution::verified::deny_wins"
status = "in-progress"
dependencies = ["OBL-0176", "INV-018"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-evolution/src/verified.rs", symbol = "peritus_evolution::verified::deny_wins", command = "cargo verus verify --package peritus-evolution --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-evolution/tests/attribution_selection.rs", symbol = "peritus_evolution::tests::attribution_selection::duplicate_objectives_are_rejected_and_unavailable_denies", command = "cargo test --package peritus-evolution --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0182"
kind = "invariant"
statement = "F0 rollback selects a distinct compatible target retained in append-only activation history under the exact protected policy and requires a new matching human approval."
owning_crate = "peritus-evolution"
source_file = "crates/analysis/peritus-evolution/src/verified.rs"
symbol = "peritus_evolution::verified::rollback_legality"
status = "in-progress"
dependencies = ["INV-019"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-evolution/src/verified.rs", symbol = "peritus_evolution::verified::rollback_legality", command = "cargo verus verify --package peritus-evolution --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-evolution/tests/verified_refinement.rs", symbol = "peritus_evolution::tests::verified_refinement::rollback_replay_and_terminal_refinements_fail_closed", command = "cargo test --package peritus-evolution --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0183"
kind = "invariant"
statement = "Every accepted F0 activation increments the pointer generation exactly once, installs the exact successor, retains the exact predecessor, and appends rather than rewrites activation history."
owning_crate = "peritus-evolution"
source_file = "crates/analysis/peritus-evolution/src/verified.rs"
symbol = "peritus_evolution::verified::pointer_conservation"
status = "in-progress"
dependencies = ["INV-019", "OBL-0182"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-evolution/src/verified.rs", symbol = "peritus_evolution::verified::pointer_conservation", command = "cargo verus verify --package peritus-evolution --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-evolution/tests/verified_refinement.rs", symbol = "peritus_evolution::tests::verified_refinement::rollback_replay_and_terminal_refinements_fail_closed", command = "cargo test --package peritus-evolution --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0184"
kind = "refinement"
statement = "Replaying each complete contiguous F0 campaign or production-pointer event prefix reconstructs the exact successor checkpoint and rejects head, prior digest, immutable binding, or successor digest drift."
owning_crate = "peritus-evolution"
source_file = "crates/analysis/peritus-evolution/src/verified.rs"
symbol = "peritus_evolution::verified::replay_refinement"
status = "in-progress"
dependencies = ["OBL-0115", "OBL-0183"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "verus-proof", source_file = "crates/analysis/peritus-evolution/src/verified.rs", symbol = "peritus_evolution::verified::replay_refinement", command = "cargo verus verify --package peritus-evolution --all-features --locked --check-toolchain --fwd-verus-args-to roots -- --no-cheating --rlimit 20" },
  { kind = "refinement-test", source_file = "crates/analysis/peritus-evolution/tests/durability_restart.rs", symbol = "peritus_evolution::tests::durability_restart::ordinary_campaign_and_pointer_commits_replay_exact_checkpoints", command = "cargo test --package peritus-evolution --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0185"
kind = "refinement"
statement = "C0 adds exact approve-once consumption to an existing multi-aggregate append in one transaction; stale registry or approval-state CAS leaves every supplied aggregate head and state projection unchanged."
owning_crate = "peritus-journal"
source_file = "crates/state/peritus-journal/src/domain/approval_use.rs"
symbol = "peritus_journal::domain::approval_use::SqliteJournal::commit_approval_use"
status = "in-progress"
dependencies = ["OBL-0115"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/state/peritus-journal/tests/approval_registry.rs", symbol = "peritus_journal::tests::approval_registry::approval_use_augments_a_multi_aggregate_append_atomically", command = "cargo test --package peritus-journal --all-targets --all-features --locked" },
  { kind = "refinement-test", source_file = "crates/state/peritus-journal/tests/approval_registry.rs", symbol = "peritus_journal::tests::approval_registry::approval_use_rejects_stale_registry_and_state_without_partial_activation", command = "cargo test --package peritus-journal --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0186"
kind = "contract"
statement = "The closed F0 campaign families 88 through 90 and production-pointer families 91 through 93 have canonical schema-one frames whose decoders reconstruct complete restart semantics and reject wrong family, unknown tag, noncanonical value, truncation, and trailing bytes."
owning_crate = "peritus-evolution"
source_file = "crates/analysis/peritus-evolution/tests/replay_wire.rs"
symbol = "peritus_evolution::tests::replay_wire::six_f0_frames_round_trip_and_reject_malformed_bytes"
status = "in-progress"
dependencies = ["OBL-0184"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/analysis/peritus-evolution/tests/replay_wire.rs", symbol = "peritus_evolution::tests::replay_wire::six_f0_frames_round_trip_and_reject_malformed_bytes", command = "cargo test --package peritus-evolution --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0187"
kind = "contract"
statement = "A2 owns an independent runtime-neutral F0 conformance catalog covering immutable evidence, change isolation, attribution, metric gaming, deterministic selection, stale authority, atomic activation, rollback history, replay, malformed input, and bounds."
owning_crate = "peritus-conformance"
source_file = "crates/app/testing/peritus-conformance/tests/conformance/evolution_catalog.rs"
symbol = "peritus_conformance::tests::conformance::evolution_catalog::evolution_catalog_runs_all_fourteen_cases"
status = "in-progress"
dependencies = ["INV-018", "INV-019", "OBL-0182", "OBL-0186"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/app/testing/peritus-conformance/tests/conformance/evolution_catalog.rs", symbol = "peritus_conformance::tests::conformance::evolution_catalog::evolution_catalog_runs_all_fourteen_cases", command = "cargo test --package peritus-conformance --all-targets --all-features --locked" },
]

[[entries]]
id = "OBL-0188"
kind = "refinement"
statement = "F0 promotion and rollback compose their decided campaign and pointer events, complete checkpoints, artifact closure, publication directive, and exact approval-use binding into the C0 atomic approval-use transaction."
owning_crate = "peritus-evolution"
source_file = "crates/analysis/peritus-evolution/src/durability/activation.rs"
symbol = "peritus_evolution::durability::activation::commit_atomic_activation"
status = "in-progress"
dependencies = ["INV-019", "OBL-0183", "OBL-0184", "OBL-0185"]
live_issue = "#23"
owner = "ACTOR-0001"
evidence = [
  { kind = "refinement-test", source_file = "crates/analysis/peritus-evolution/tests/promotion_authority.rs", symbol = "peritus_evolution::tests::promotion_authority::authorization_digest_binds_every_authority_observation", command = "cargo test --package peritus-evolution --all-targets --all-features --locked" },
]
