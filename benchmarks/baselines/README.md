# Accepted baselines

No baseline is established in source by this slice. A baseline document conforming to
`../schemas/baseline-v1.schema.json` may be admitted only after its source H3 evidence is retained and
reviewed. Its `evidence_digest` binds the exact accepted evidence manifest; hand-authored or
criterion-generated point estimates are not accepted baselines.
