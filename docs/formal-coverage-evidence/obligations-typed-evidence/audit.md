# Typed obligation evidence correspondence

This increment follows the binding and performance correction and is a bounded source review,
not an obligation discharge. The five reviewed source identities are in `source-sha256.txt`.
The independent Sol reviewer inspected all five source files, contracts, production callers and
parent-produced execution logs; the reviewer did not independently execute the compiler.

## Proved correspondence

Browser evidence requires the exact standards-compliant implementation, a present matching
oracle digest and a passing observation. Lifecycle evidence requires public ingress and all
four exact identity digests. Direct evidence returns its supplied satisfied flag. External-effect
evidence requires the exact effect digest, public boundary and completed observation; the actual
qualification branch now calls that verified reducer. Constructors and getters expose those
stored fields exactly. Manual Clone contracts preserve every field and complete binding content.
RequirementEvidence selects the actual variant binding and requirement identity exactly; Schema
adds the exact binding view and getter only.

## Verification and review

Isolated and integrated strict pinned Cargo-Verus runs completed 193 checks with zero errors.
All 16 ordinary package tests passed in both trees. Isolated all-target/all-feature Clippy with
warnings denied and package formatting passed. Exact commands and complete logs are retained
beside this report. Nine existing derived-Clone warnings remain in the obligation package;
two additional warnings in the Verus log belong to its settlement dependency. This increment
removes four obligation Clone warnings by supplying complete semantic contracts.

## Remaining boundaries

Observation truth, public ingress and external execution authenticity remain supplied input
assumptions. Schema direction/field coverage and constructor semantics, the derived
RequirementEvidence enum Clone, and the overall qualification lookup, traversal, alternatives,
aggregation and final report still need implementation-to-specification proofs. Exact leaf
contracts do not establish those caller guarantees. The reviewed checkpoint records neither a
new exclusion nor a claim that the complete obligation scope is finished.
